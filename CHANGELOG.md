# Changelog

## 2026-08-14（全面更名：Sculptor → Stylotrace）

- 全量改名：CLI 命令 `stylotrace`、技能触发名 `name: stylotrace`、环境变量 `STYLOTRACE_*`、安装路径 `~/.codex/skills/stylotrace` 与 `~/.local/share/stylotrace`；
- 论文 `科技论文-Stylotrace.md/.docx` 及配套图表脚本同步更名；
- 本机安装已迁移：`~/.codex/skills/sculptor` → `stylotrace`、`~/sculptor` → `~/stylotrace`、`~/.local/share/sculptor-agent` → `~/.local/share/stylotrace`。

## 2026-08-13（v1.0 商业化收口：词级文体计量 + 改迹变换 + 拟改层 + 消融）

从"候选级评分原型"升级为"可解释、可学习、可复现作者改法"的完整链路：

- **词级文体计量（stylometry）**：功能词 + 标点节奏（Burrows Delta 思想）替换字符 n-gram，
  作者识别 46.3% → 76.4%；surface 从绝对规则改为相对作者节奏，impedance 去噪；
- **改迹变换（第 13 维）**：从编辑对学"作者新增/删除什么"，正空间（加）+ 负空间（删）；
- **拟改层**：负方向 `applyAuthorEdits`（删作者删过的连接词/AI 腔 + 断句），正方向 `concretize`
  （用作者编辑对 few-shot 做"抽象→具体"拟改），从"选优"升级为"选优 + 拟改"；
- **小样本冷启动**：个人语料阈值从 200 字符降到 ≥1 条编辑对即启用对比解码；
- **评测基础设施**：配对置换检验（`permutationTestPaired`）、作者识别 v2（真实 JS 签名 + 强基线 +
  置换检验）、调制器消融（学习 > 默认、逐维归因、噪声清零）；
- **V2 logprobs 探测**：DeepSeek V4 只返回 `reasoning_content`（思考链）不返回 `content`（成品），
  逐 token 重排按既定策略文档化降级；
- agent 24 套 + web 11 套全绿；版本统一 1.0.0。

## 2026-08-13（v0.68 改迹调制做实：修改即标注 + 可解释 + 学习曲线）

按批准的升级方案（UPGRADE-PLAN.md Phase 1）执行，命名按用户要求不造生硬缩写：

- **改迹调制（revision-trace modulation）**：论文核心从"统一 Token 对比解码"校准为
  "从作者修改中学习"的候选评分机制，摘要/贡献/方法/结论全面重排（贡献一改为改迹调制）；
- **个人回避库（负空间第一阶段）**：`agent/src/avoidance.js` 聚合"作者亲手删掉的词"，
  调制器第 12 维特征，命中即压低；
- **上下文窗口编辑对**：point-edit 记录修改点前后文，训练时用带上下文的偏好对，
  缓解"短替换特征稀疏"；
- **可解释层**：`contributionBreakdown` + `humanRationale`——得分分解翻译成人话
  （"为什么选它"），写作评分路径可用；
- **样本复杂度实验（A3）**：`scripts/experiments/rsa-learning-curve.mjs` + 绘图——
  留出得分边距 0.286→0.498、权重稳定性 0.968→0.999 随 n 单调改善，n≈15–20 进入平台，
  论文新增 3.1.2 与图 8；
- **盲评统计（D1）**：`agent/src/stats.js` 精确二项/Wilson CI/Cohen's h/CSV 解析，
  CLI `stylotrace experiment blind-stats <answers.csv>`；
- **embedding 统一**：`style-vector.js` 删除重复实现，统一走 `embedding.js`；
- **doctor 检查**：新增神经风格编码/外层调制器/作者写作清单检查项；
- 新增 `test/stats.test.mjs`（五组断言），agent 24 套 + web 11 套全绿；
- 论文 docx 重渲染、计数修正 agent 24。

## 2026-08-13（v0.67 姿态层细读判据前置：从"事后审计"到"事中选择"，软性加权）

继续补劣势，并按用户要求**不做硬约束**：

- **调制器第 11 维 posture**：把"假思考六层细读"的确定性判据（金句排比收束/路标转折/
  点题顿悟，`deterministicFakeThinking`）转成 0~1 健康度特征——表演式文本在候选排序时
  即被压低，克制式文本得分高；**软性加权，只影响排序与审计报告，不拒绝生成**；
- 写作评分、得分分解与降级路径自动包含 posture（零额外成本，确定性计算）；
- modulator.test 新增姿态层断言组（表演式低/克制高/软性不拒绝/分解可追溯）；
  agent 23 套 + web 11 套全绿；
- 论文：实现边界/5.7/6.6/局限/结论/未来工作升级十一维 + posture 前置，
  明确"软性引导、作者保留否决权、无拒绝式硬约束"；计数 agent 23。

## 2026-08-13（v0.66 L3 作者写作清单：深层风格读取结构化，直接参与生成评分）

继续补劣势：把"深层风格读取依赖用户配合"补齐为"已确认信号自动归纳 + 直接入评分"。

- **新增 `agent/src/author-sheet.js`（作者写作清单协议）**：五问（主张/论证/读者/红线/触发）
  从 state 的澄清信号、外溢种子、红线、思想脉络自动归纳；LLM 优先输出结构化 JSON，
  失败确定性兜底；**红线拆句后强制保留**（LLM 归纳漏词也不丢）；
  落盘 `vault/author-sheet.json`，签名变化自动重算；
- **调制器第 10 维 fineRead**：候选文本对作者清单红线片段/关键词的命中进入评分，
  无清单时中性 0.5 降级；得分分解新增 fineread 字段；
- **CLI**：`stylotrace author-sheet [--refresh]` 查看/强制重算五问清单；
- 新增 `test/author-sheet.test.mjs`（确定性兜底/LLM 归纳/红线保留/签名重算/fineRead），
  agent 23 套 + web 11 套全绿；
- 论文：实现边界/5.7/6.6/局限/结论/未来工作升级到十维 + L3 作者清单，表 A1 新增
  author-sheet 行，计数修正为 agent 23 + web 11。

## 2026-08-13（v0.65 神经风格编码与检索补强 + 论文差异化优势）

用户要求：论文突出强调优势、劣势积极补上、暂不做商业化。本轮把上一轮诚实盘点出的
功能短板逐项补强，并把补强写进论文：

- **神经级风格编码**：新增 `agent/src/embedding.js`——配置 STYLOTRACE_EMBED_* 后，
  作者语料编码为稠密语义原型（分块均值、落盘缓存、签名变化重算），作为调制器
  **第 9 维 embedding 特征**与候选文本做余弦；未配置时全部静默降级；
- **知识库向量混合检索**：`matchKbHybrid`——BM25 + 语义余弦 0.6/0.4 融合，
  条目向量落盘缓存（vault/kb-vectors.json），分数可追溯；无 embedding 自动降级纯 BM25；
- **调制器增量在线更新**：`applyEditIncremental`——新编辑对到达时局部 SGD 更新权重，
  避免全量重训，"越写越懂你"即时生效；
- **decodeSection 接入**：写作候选评分自动使用原型 + 候选 embedding（无配置零开销）；
- 新增 `test/embedding.test.mjs`（原型/第 9 维/语义排序/增量更新），agent 22 套 + web 11 套全绿；
- **论文升级**：6.3 新增"差异化优势与人文价值"（表 7 六维行业对比：Sudowrite/Claude/ChatGPT/
  Grammarly/通用助手 vs Stylotrace）、6.6 新增"针对行业短板的补强"、局限与未来工作同步更新，
  附录表改规范编号（表 A1/A2）；docx 重新渲染；
- CLI：`stylotrace knowledge search` 配置 embedding 后自动启用语义混合检索。

## 2026-08-13（v0.64 外层调制器：签名正式升级为可学习模型）

用户要求：把"外层调制器"路线走通——个人知识库、风格向量、收集到的数据统一归纳进
调制器，动态收集纯净数据做小数据微调，让它在风格获取与影响大模型方面有突破。

- **新增 `agent/src/modulator.js`（外层调制器）**：八维可解释特征函数（个人 n-gram、
  表层、话语、立场红线、知识、缺陷、阻抗、风格向量方向），统一承载个人知识库、
  风格向量与收集数据；
- **签名 → 模型**：调制器权重不再手调——edits.jsonl 的（原文，改后）作为作者偏好对，
  pairwise hinge + SGD（z-score、L2、权重裁剪、确定性）拟合每个作者独有的权重向量；
  权重可解释 = 作者在该维度的坚定度/回避度；
- **纯净数据动态收集**：风格样本(1.0)、编辑后文本(0.7)、归档作品(0.4)、成稿(0.4)
  为正例；编辑原文为负例；检索回灌与未经确认的 LLM 文本不进风格语料；
- **在线重训**：数据签名变化自动失效重训，权重落 `vault/modulator-weights.json`；
  无编辑对时自动降级经验默认权重，不阻塞写作；
- **接入推理**：`token-decode.js` 升级 V1.5——写作每节并行候选 → 八维调制评分选优 →
  得分分解（八维特征 + 权重 + 训练标记）；所有走 decodeSection 的路径自动生效；
- **CLI**：`stylotrace modulator [--train] [--export] [工作区]`；
- 新增 `test/modulator.test.mjs`（数据收集/权重学习/八维分解/降级/在线重训），
  agent 套件 21 套 + web 11 套全绿；设计文档 `docs/MODULATOR.md`；
- 论文同步：摘要/C4/3.1.1/5.7/6.2/6.4/7/8 升级为"V1.5 外层调制器已落地"，
  测试计数修正为 agent 21 + web 11。

## 2026-08-13（v0.63 完整风格体系设计：表层指纹 → 深层选择模式 → 解码期控制）

用户要求：从人类风格形成与深层风格读取出发，整理一套完整的风格体系（表层/深层），
并采用解码期控制等高级方法。本轮以设计文档固化理论，并同步论文：

- **新增 `docs/STYLE-SYSTEM.md`**：四层风格体系（L1 表层词汇句法 Burrows Delta / L2 中层话语修辞
  Biber MD / L3 深层认知立场 Author Writing Sheet / L4 元层选择偏好 Flower & Hayes + GhostWriter），
  每层给出理论依据、信号来源、读取方法、语料需求与现有资产映射；
- **深层风格读取协议**：小语料下深层风格不靠统计、靠交互确认——作者写作清单五问
  （主张/论证/读者/红线/触发）系统化 L3 读取；
- **解码期控制路线**：DExperts 专家/反专家对比、对比解码、PREADD、ITI 激活干预、
  Steering Vectors / StyleVector、偏好对权重学习（edits.jsonl → λ/β/R），映射 V1（已落地）/V2/V3；
- **验证协议升级**：按 arXiv:2508.06374 的三类判别任务组织验证，明确"作者识别显著超过 TF-IDF
  基线（当前 81.7% vs 82.2%）"为必须攻克的硬指标；
- 论文同步：3.2 新增"体系化：从表层指纹到深层选择模式"、未来工作补解码期控制路线与
  权重学习、参考文献新增 [14]–[24]；测试计数修正为 agent 20 + web 11 套。

## 2026-08-13（v0.62 统一 Token 对比解码 V1 落地：签名 → 个人模型）

回应"框架只是路线图、风格只是签名"：把统一解码框架 V1 真正实现，并把签名升级为
可预测作者下一步选择的个人模型：

- **个人模型（p_personal）**：`agent/src/personal-model.js`——基于作者本人语料
  （风格样本/成稿/亲手修改后的文本）训练字符级 n-gram 条件模型（order=4，回退+加一平滑），
  输出每字符对数概率；与知识库严格分离（"如何写"来自作者语料）；
- **候选对比解码（V1）**：`agent/src/token-decode.js`——写作每节并行生成 2 个候选，
  五路信号评分选优：S = 2.0·p_personal + 0.5·S_knowledge + 1.0·S_defect + 0.8·R(t)·S_impedance；
  得分分解（personal/defect/knowledge/impedance）逐候选可追溯；
- **写作节级接入**：writeSection 走 decodeSection，`STYLOTRACE_DECODE_N` 可调；
  无个人语料（<200 字符）自动降级为直接生成，不增加延迟与成本；
- 回归：agent 20 套（新增 token-decode.test：个人模型可预测/缺陷与阻抗信号/对比选优/
  无语料降级）+ web 11 套全绿；
- 论文同步更新：3.1.1/5.7/6.2/7/8 的"解码层路线图"改为"V1 已落地"，逐 token 重排
  （V2 logprobs / V3 本地 LoRA）如实保留为未来工作。

## 2026-08-12（v0.61 Web 批注：选段批注 → 收集 → 一键 AI 修改）

把"在文档上选段写批注、批注被收集并交给 AI 处理"的注释体验搬进 Web 端：

- **选段批注**：成稿预览与作品弹层里选中文字即出现批注入口，写下意见即落库
  （vault/annotations.jsonl，含原文锚点/批注/状态）；
- **批注面板**：成稿页与作品弹层各有批注列表——定位到原文段落（闪烁高亮）、删除、
  已应用标记；按文件过滤；
- **一键 AI 按批注修改**：把该会话未处理的草稿批注逐条交给定点修改引擎
  （找不到原文时自动尝试加粗/代码包裹变体），应用后草稿实时刷新、批注标记"已应用"；
- 接口：POST/GET/DELETE /api/annotations + POST /api/annotations/apply；
- 回归：web 11 套（新增 annotations-qa：保存/读取/过滤/应用/删除）+ agent 19 套全绿；
  真实 LLM 实测"改成口语短句"批注被正确执行。

## 2026-08-12（v0.60 作品同步 / 分割 / 全流程互操作）

用户要求：作品同步、分割、以及全流程与其他流程的接入，必须解决。

- **作品导入与分片**：/api/works/import 支持 text/文件，长文按 Markdown 标题 +
  6000 字自动分片入库；Web 作品页新增"导入作品"；
- **作品同步与管理**：/api/works/sync 一键归档各会话最新草稿；/api/work
  （改名/分类）+ DELETE（删除）；外部 Word/其他 Agent 作品可进入同一作品库，
  继续对比/审计/导出；
- **成品文档接续**：/api/import-draft 把成品 md/docx 导入会话成草稿，
  立即可 审计/导出/风格重写/继续写作；Web 作品卡"继续"一键完成；
- **长文档分段审计**：学术规范审计按标题/6000 字分片逐段 LLM 审阅、合并去重，
  不再截断漏检后半部分；
- **按阶段导出**：/api/export 新增 what=draft|outline|report|style|knowledge，
  每个环节都能"文件出"（md/docx/pptx）；
- 底层：agent/src/io.js 新增 splitLongText；library.js 新增 importWork；
- 回归：web 10 套（新增 works-qa：导入分片/同步/改名/删除/导入草稿/导出）+ agent 19 套全绿。

## 2026-08-12（v0.59 问询系统·外溢优先：动态规划 + 提示词工程）

依据 docs/问询系统升级-v1.md 升级问询系统：用户主动说出的、系统本来不准备问的
高价值信息，从"当普通素材归档"改为"当轮接住、回显、深挖、入档、重排"。
按要求**归纳规律、不硬编码**——分类与深挖由 LLM 动态决定，确定性只做兜底：

- **外溢优先（规则 12，最高优先级）**：追问设计师每轮输出 overflow 字段
  （type: reference/personal/constraint/reasoning + seedText/constraint/coreThesis），
  检测到外溢就当轮复述确认＋深挖一问，连续最多 2 轮后回主线清单；
- **种子/红线入档**：state 新增 `seeds`（含类型/确认状态）、`constraints`（红线清单）、
  `coreThesis`（立意种子）、`overflowLog`；大纲与后续写作注入"种子必须兑现、
  红线一字保留"；短句确认（"对，就是那个"）把种子标记为已确认；
- **升级信号流水**：每次外溢自动追加 `vault/overflow-log.jsonl`；
  `scripts/overflow-summary.mjs` 聚合生成 docs/questioning-upgrades.md 反哺问询系统；
- **问法留出入口（提示词工程）**：素材/立场/论证/约束的收集式模板改写为
  "现成线索/触发点/现成推理线/定死的红线"式开放问法（实测首问即
  "是什么让你现在想写——某个画面、一句话、一个人，还是某件相关的事"）；
- **Web 可见**：上下文面板新增"种子与红线"区，/api/context 返回 seeds/constraints/
  coreThesis/overflowLog；
- 确定性兜底（LLM 不可用时）：只抓最明确的《书名》与红线短语，分类交给下一轮 LLM；
- 回归：web 9 套 + agent 19 套（新增 overflow-qa）全绿；
  实测悼念场景红线"他最后笑了笑，没说话"当轮入档 + AI 深挖"这句之前攒下了什么"。

## 2026-08-12（v0.58：问询系统升级——接住"用户主动说出、系统原本不准备问"的维度）

历次深度写作对话复盘发现：用户多次主动给出系统阶梯没有对应问法的内容——触发作品
（小约翰可汗《硬核狠人》/《局外人》/《乡土中国》）、可借用的现成讲述（半拿铁播客）、
私人连线（父母博士导师是当年人民银行副行长）、外部意见（"两位作家建议"）、深挖指令
（"深挖内核"）、加速授权（"你直接开始写吧"）。本轮把这些变成问询系统的一等公民：

- **蓝图新增可选字段**（genre.js）：`trigger` 写作触发点、`connection` 私人连线、
  `borrow` 可借用讲述、`externalInput` 外部意见——挂在散文/演讲稿/悼念词/小说/议论文
  等蓝图里，清单可见、答或不答都不卡门槛。
- **新增文体「悼念词」**（祭文/追悼词/纪念词/送别词）：对象与渊源先立、引语只用品行
  出处、不空喊悲痛；蓝图自带触发点/私人连线/借用讲述字段（本轮《悼朱镕基先生》实战验证）。
- **追问设计师新规则**（QUESTIONER_PROMPT 10.8–10.13）：触发源优先挖"它打动你的那一点"；
  私人连线问"这条线对你意味着什么"；借用讲述问"借内容还是借口吻"并承诺核对原样；
  外部意见问"哪些你认同、哪些不认同"；深挖信号按思想脉络追因果最深层；
  加速授权（"直接开始写吧/剩下的你定"）识别为主动推进而非低意愿——记录加速、停止追问、
  剩余维度按建议默认。
- **澄清状态机**（clarify.js）：VALID_INTENTS/NEED_LABELS/兜底问题补齐四个新维度；
  新增加速信号识别（ACCELERATE_RE），触发时记 `state.accelerated` 并 markDeferred。
- **协议文档**（references/questioning.md）：新增"用户主动说出的问题"清单、
  "加速授权（区别于低意愿）"与"问题质量反馈（收尾问一次哪个问题最戳中你）"。
- 附带（工作区已有）：AI 主导知识筛选（captureKnowledgeAI）、用户确认低置信条目升级。

## 2026-08-12（v0.58-web：会话隔离+联网查验+AI知识库+去登录）

Web 端本轮（与上面的问询系统升级同一版本号，互相配合）：

- **登录移除**：删除服务端鉴权与前端登录遮罩，`/api/auth/status` 恒返回
  `{required:false,ok:true}`；公网实例如需防护建议走反向代理；
- **会话隔离与进度可见**：每个会话独立工作区（协议/素材/风格/知识互不串扰）；
  项目列表直接显示各会话 文体/大纲节数/素材条数/已确认项/风格底稿；
- **联网检索开箱即用**：内置免费检索（必应中国 → 百度 → DDG → 维基，视频类优先
  B站 API），Web 端默认 `STYLOTRACE_SEARCH_PROVIDER=builtin`；"帮我查一查"直连检索、
  结果（标题/链接/摘要）回灌素材并标注"来源待确认"；查询词自动清洗（《书名》优先）；
- **AI 主导知识筛选入库**：看过/读过/在B站/新闻/去过 → LLM 提炼（书/视频/新闻/
  文章/地方/观点）入库，正则只做入口；短句确认把低置信条目升级为"已查验"；
  知识库页面升级为跨会话聚合的个人知识库；
- **翻译等工具融入**：文档翻译/风格重写/学术审计走真实 LLM 管线，可导出 md/docx；
- **风格多面读取**：风格提炼语料并入知识库条目（读/看过什么=口味的一部分），
  与旧稿、修改记录、对话共同构成风格来源；
- 回归：web 9 套 + agent 18 套测试全绿；实测无登录直连、B站知识入库、联网检索
  回灌、翻译导出均正常。

## 2026-08-12（v0.57 实时大纲可视化 + 澄清去模板化 + 响应提速）

用户实测反馈："新闻"开场被漏判、冒出"发文依据/主送对象"类模板傻问、大纲不实时、
API 响应太长、用户反问/质疑时系统只会继续按模板问。本轮逐根因修复：

- **文体识别补词**：`新闻稿` 别名补齐"新闻/通讯/特稿/快讯/深度报道"，
  "我想写一篇新闻"不再漏判成默认文体（根因 1：走错澄清蓝图）；
- **模板傻问根因修复**：追问 JSON 被 max_tokens=1000 截断 → 解析失败 →
  整段丢掉换成模板兜底（根因 2）。maxTokens 提到 2500，`parseJsonContent`
  增加宽容提取，能抢救被截断的 question/intent/recommendation；
- **移除"软性拉回"**：不再用蓝图缺口硬换 LLM 的问题（根因 3），
  由 LLM 决定问什么，重复问题仍由重复护栏兜底；
- **LLM 声明意图（intent）**：追问 JSON 新增 intent 字段，用户回答按 LLM
  声明的维度入库，正则分类只做兜底——"问得自然、记错字段"的反复追问消失；
- **低意愿误吞修复**：`LOW_WILL` 由子串匹配改整句精确匹配，回答中含"够了"
  （如"站一会儿就够了"）不再被当成"用户不想答了"而丢弃（根因 4）；
- **用户反问/质疑接得住**：反问不再当答案记录，LLM 先正面回答再轻轻拉回
  （LLM 不可用时给"帮助式"说明，不再硬套模板）；
- **实时大纲每轮必长**：LLM 未给结构节时，按已确认内容自动生成
  立意/素材/论点/结尾/读者等内容节（LLM 出真结构后接管）；对话内联迷你大纲卡、
  右侧面板更新闪烁、大纲变化 toast、思考提示轮换；
- **响应提速**：意图理解与提问并行刷新（首轮仍串行）、短回答轮跳过意图重提、
  Web 端默认超时 120s/重试 2 次（CLI 端保持 300s/4 次）；
- **读者/对象合并**：非公文文体中"读者/受众"与"发布对象/当事人"视为同一维度，
  答了其一不再反复追问；
- 回归：web 9 套 + agent 18 套测试全绿；实测新闻开场 → 报道角度 → 素材 →
  结尾 → 读者全流程无模板问句，单步延迟较优化前约减半（API 波动仍受上游影响）。

## 2026-08-12（v0.56 大纲与上下文读取核验）

用户反馈"大纲不一定能正常读取、已有对话上下文要如实分析"，逐链路核验并补回归测试：

- **大纲链路实测**：mock 完整对话流后，`/api/context` 如实返回 liveOutline（3 节）、
  完成度 100、大纲标题与节数一致；前端列表/图谱双视图、卷级分组、点击定位草稿逻辑核对无缺陷；
- **思想脉络如实提炼**：修复 mock 缺少"思想对话者"分支导致演示/离线模式下思想脉络恒为空——
  补 mock 提炼（主张/前提/推理/来源/可深挖），真实推理链回答后上下文面板如实呈现；
- **上下文分析核验**：素材条数、回答分级（L0–L5 统计）、思想脉络均有断言覆盖；
- 新增 web/test/context-qa.mjs（实时大纲/完成度/素材/分级/思想脉络 6 项断言），
  web 套件由 8 套增至 9 套、309 项断言全绿；agent 21 套全绿。

## 2026-08-12（v0.56 Web 升级：工具面板 + 生产级鉴权）

- **Web 新增"工具"面板**：学术规范审计（对当前会话成稿执行，LLM 深审 + 确定性兜底）、
  文档翻译与文档风格重写（上传 docx/md/txt，docx 自动走块级回填保留 run 级格式），
  产物可下载 md / docx / html；
- **生产级鉴权**：设置 `STYLOTRACE_WEB_PASSWORD` 后启用登录保护（HttpOnly Cookie，
  12h 会话，/api 全部拦截，前端自动显示登录遮罩）；未设置保持单机免登录兼容；
- **部署文档**：docs/INTEROP.md 新增 Web 部署章节（环境变量、HTTPS、数据备份、密钥安全）；
- 测试：新增 web/test/tools-qa.mjs（鉴权流程 / norm / doc translate+下载 / doc restyle / 登出拦截），
  web 套件由 7 套增至 8 套、303 项断言全绿；真实端口冒烟（登录 401/放行、翻译导出）通过。

## 2026-08-12（v0.56：文档互通管线——全流程"文件进、文件出"，与其他产品衔接）

调研行业最佳实践（Pandoc DOCX↔Markdown 往返、MCP 文档工具生态、MinerU/Docling 高保真解析）后落地：

- **新增 `agent/src/doc-pipeline.js`**：以 Markdown 为规范中间表示（canonical IR）。
  - `docTranslate`：docx/md/txt → 原意解读（intent/tone/genre/keyImagery/pitfalls）→ 结构保留翻译
    → md/docx/html 导出 + 回译校验；
  - `docRestyle`：成品文档按作者风格重写（style 支持旧稿文件/方向描述/工作区档案），结构保留导出；
  - 无密钥确定性兜底：导出原文 md 并提示。
- **CLI**：`stylotrace doc translate|restyle <文件> [--lang] [--style] [--out]`；
- **MCP**：新增 `doc_translate` / `doc_restyle` 工具，任何 MCP 客户端可调用；
- **run 级格式保留（v0.56 追加）**：`.docx` 输入走 `scripts/io/docx_blocks.py` 块级管线——按文档顺序
  提取段落/表格单元格（P0、T0_R0_C0_P0）→ LLM 逐块翻译/重写（块数与 ID 一一对应、失败块单块重试、
  仍失败保留原文）→ python-docx 写回原段落首个 run（保留字体/加粗），段落样式与表格结构原样保留；
- **文档**：docs/INTEROP.md——设计原则、导出矩阵、与 Word/Pandoc/其他 Agent 的衔接方式、批量处理；
- 测试：doc-pipeline.test.mjs 5 组（md 翻译/重写/兜底 + **docx 块级翻译与重写的 run 级格式保留实测**）；
  agent 套件 21 套全绿。

## 2026-08-12（v0.55：学术规范审计——把"格式/语言专项评审"标准做进 Stylotrace）

模拟大赛评审给出的"语法、语句、格式与学术规范"专项意见，直接固化为系统能力，
让 Stylotrace 对自己的成稿做同样标准的检查：

- **新增 `agent/src/academic-norm.js`**：学术规范审计——中英标点混用（半角括号紧贴
  中文、英文逗号后直接中文）、正式文体口语化（学术论文/报告/公文才触发，引号内
  不误伤）、摘要超长（>320 字）、关键词不规范（数量与领域通用性）、引用编号顺序
  （顺序编码制首次出现递增与编号连续）、图表顺序引用（图/表独立序列）。
- **LLM 优先、确定性兜底**：`NORM_PROMPT` 按顶级会议评审标准做四类深审
  （language/terminology/format/structure），确定性检查结果作为"待复核疑点"注入；
  无密钥或 LLM 失败自动降级，只报告不擅自改稿（修改仍走定点修改）。
- **质量门静默接入**：交付前 `state.quality.norm` 记录问题数与高优先级数；
  学术文体且配置密钥时自动触发 LLM 深审，不阻塞交付。
- **CLI**：`stylotrace norm [--file x.md] [工作区]`，报告落盘 `vault/norm-report.md`。
- 测试：academic-norm.test.mjs 8 组——确定性六类检查 + LLM mock 全链路 + 静默路径；
  agent 套件由 19 套增至 20 套。

## 2026-08-11（v0.53：假思考检测去硬编码——LLM 六层细读 + RAG 作者对照）

用户要求"把硬编码改成 RAG/LLM，充分发挥模型能力"。v0.52 的确定性正则升级为主路径之外的兜底：

- **新增 `agent/src/fake-thinking.js`**：LLM 六层细读——voice（叙述者语言与设定矛盾）/
  transition（过渡公式化与元叙述）/ rhetoric（排比对仗语义空洞）/ citation（检索式引用）/
  translate（"翻译过来就是"式解释代替呈现）/ ending（金句收尾空转），输出
  score + 病灶（含 quote/problem/fix）；诊断前 **RAG 检索作者 persona / 个人写作 skill /
  知识库来源**作"声音像不像设定里的人"的参照。
- **确定性正则降级为兜底**：无 key 或 LLM 失败时自动降级（`deterministicFakeThinking`），不中断。
- **redteam 主流程接入**：LLM 细读前置到红队修复（让修复看到姿态层问题并带上修法），
  修复后复查；结果写 `state.quality.fakeThinking` 供 Web `/api/report` 展示。
- **CLI**：新增 `stylotrace diagnose [--file]`；Web `/api/report` 返回 fakeThinking。
- 测试：fake-thinking.test.mjs 扩展至 7 组——LLM 六层细读（mock 返回 voice/ending 病灶）、
  无 key 兜底、细读提示词 RAG 作者对照注入；agent 19 套 + web 7 套 QA 全绿。

## 2026-08-11（v0.52：AI 假思考全面根治——前置禁令 + 姿态层检测 + 回归样例）

## 2026-08-11（v0.52：AI 假思考全面根治——前置禁令 + 姿态层检测 + 回归样例）

用户实测反馈：《差生》是 v0.17 写的（简洁提示词 + 反假大空 + 具体细节），后来改差了。
git 考古定位根因：结构性检测保留在事后审计，但写作提示词被大量注入块稀释，
"反假思考"的正向纪律没有前置。从四个层面根治：

- **提示词前置禁令（写作/扩写/修复三层）**：反金句收束（"…是…，是…，是…"式同义反复）、
  反表演思考（"后来我想/但这里头有个悖论"式路标转折）、允许冗余与模糊（场景不必全服务
  论点）、不替人物开口（失语就让他失语）、反过度释义（读者能懂的不要写出来）。
- **姿态层检测（redteam.js）**：新增 `fakeThinking` 字段——金句排比收束、路标式转折（≥3）、
  点题式顿悟（≥2），并入审计建议与红队修复问题清单。
- **红队修复要求**：假思考痕迹按作者风格改得更笨拙具体，绝不用新金句替换旧金句。
- **回归样例**：`agent/test/fake-thinking.test.mjs`——《语言匮乏》式金句结尾命中、
  《差生》式克制结尾（"他叫我同学。我没有纠正他。"）不误伤；提示词三层禁令断言。
- 论文/THEORY：新增"假思考检测"小节，诚实记录"统计指标抓不住姿态层假思考"的短板
  与 v0.52 的解法（《语言匮乏》黑名单 0 / σ17.1 / TTR0.59 仍"没心跳"的实证）。
- 测试：agent 18 套 + web 7 套 QA 全绿；引擎同步。

## 2026-08-11（v0.51：翻译方法论（原意优先）+ 数学公式特殊格式输出）

## 2026-08-11（v0.51：翻译方法论（原意优先）+ 数学公式特殊格式输出）

- **翻译以理解作者原意为第一标准**（roundtrip.js）：新增【原意解读】步骤——
  翻译前先读懂"作者想表达什么/语气/文体/关键意象/易损点"，原意块注入正向翻译与
  回译提示词（达意优先于逐字）；报告首行输出"原意理解"，只校验不擅自改稿。
- **数学公式等特殊格式**：`stylotrace export --latex out.tex` 把 md（含 $$…$$）导出为
  LaTeX 论文（公式原样、标题映射、特殊符号转义）；`scripts/formula-render.py` 用
  PIL+Unicode 数学符号渲染 4 张核心公式 PNG（环境无 LaTeX/matplotlib 时的可嵌入方案），
  嵌入 STYLE-MATH.md。
- **论文/THEORY**：新增"翻译方法论"章节（3.2.5 / 2.4）——先懂后译、达意优先、
  回译校验闭环、区分翻译损耗与原文模糊。
- 测试：roundtrip 增加原意断言 + LaTeX 导出断言；agent/web 全量回归。

## 2026-08-11（v0.50：多维风格向量三图 + 严格数学推导 + 多工具对比）

- `scripts/style-vectors.py` 生成三张图 + `STYLE-MATH.md`：
  - 图1 多维风格向量热力网格（8 维 × 6 对象）；
  - 图2 风格距离矩阵（欧氏距离，公式 d(A,B)=√Σ(v̂ⱼᴬ−v̂ⱼᴮ)²）；
  - 图3 写作能力全方位对比（5 维加权综合评分 C=Σwₖsₖ，Stylotrace 实测 vs 通用模型/模板基线）。
- 对比对象：Stylotrace 作者、人类名家（史铁生/费孝通）、ChatGPT/DeepSeek 通用基线、模板公文基线；
  实测：Stylotrace↔名家 1.11/1.54，↔通用模型 1.46/1.49，↔模板 1.98；通用模型彼此仅 0.65（AI 腔趋同）；
  综合能力 C：Stylotrace 87.4 vs 38.6/38.6/34.0。
- 论文与 THEORY.md 同步（docx 内嵌新图 2），删除旧 3 对象图避免数值打架。

## 2026-08-11（v0.49.1：术语通俗化 + 风格向量对比可视化）

- 术语优化："困惑度签名"→"语言新鲜度（不可预测性）"，"偏好对"→"亲手修改记录"，
  行文保留 AI/数学学术框架（信息论高熵、特征空间等）。
- `scripts/style-visual.py` + `style-vector-compare.png`：8 维可解释风格特征
  （节奏/口语/意象/情绪/词汇/新鲜度）对 Stylotrace 作者、人类名家（史铁生《我与地坛》）、
  ChatGPT 三类文本做归一化向量，输出雷达图 + 欧氏距离矩阵 + 距离对比条；
  实测距离：Stylotrace↔名家 1.48、Stylotrace↔ChatGPT 2.02、名家↔ChatGPT 2.05。
- THEORY.md 与参赛论文 md/docx 同步更新（论文 docx 内嵌对比图）。

## 2026-08-11（v0.49：真实 LLM 可靠性修复 + 理论架构文档）

论文写作阶段用真实 DeepSeek 跑导演全流程，暴露两个真实模型问题并修复：

- **大纲 token 截断**：7500 字论文大纲生成时 JSON 被 3000 token 截断 → 解析失败。
  修复：outline.js 预算 3000→6000 + temperature 0.5 + 解析失败自动"补全"重试一次。
- **澄清不主动收尾**：真实 LLM 问完所有字段后不遵守 stop 规则、继续追问。
  修复：导演侧"确定性收尾护栏"——蓝图字段全部确认后，无缺口仍被追问 ≥2 轮
  即强制放行进大纲（放导演层，interview 等独立流程的完成语义不受影响）。
- 新增 `agent/test/completion-guard.test.mjs`（模拟永不停问的 LLM，验证两轮放行 +
  大纲生成）；agent 16 套 + web 7 套 QA 全绿。
- **docs/THEORY.md**：完整理论架构——Agent（导演状态机/多 Agent 协作/双形态）、
  人工智能（风格=结构化偏离/思想脉络/三重保真）、数学（风格轨迹/人类化指标/
  对照消融盲评/状态机不变量）、工程（零依赖/QA 体系/Web 工作台/安全退让），
  附与竞品的"绝对优势"对照表与人文价值落点。

## 2026-08-11（v0.48.1：Web 接口自检——零断链确认 + 死元素清理）

## 2026-08-11（v0.48.1：Web 接口自检——零断链确认 + 死元素清理）

- 静态盘点 app.js 全部 API 调用 vs server.mjs 全部路由：**30 个调用路径零断链、
  29 个唯一调用点 HTTP 方法全部一致**；`/api/rag/needs` 属供给协议端点
  （宿主/学术 agent 用），Web 经 context 展示待办数，无功能缺口。
- 清理死元素：`modePill`（"离线演示"静态标签，JS 零引用）移除，版本角标 v0.29 → v0.48.1。
- 留档 `docs/WEB-API-AUDIT.md`（接口清单 + 交互覆盖矩阵）。
- web 7 套 QA 全绿；版本 0.48.1，引擎同步。

## 2026-08-11（v0.48：Web P2 可视化——大纲图谱 + 多作品对比）

## 2026-08-11（v0.48：Web P2 可视化——大纲图谱 + 多作品对比）

- **大纲图谱**：大纲面板新增 列表/图谱 双视图——图谱按卷分组渲染"卷→节"卡片连线，
  每张卡显示 序号/标题/功能/论点/字数/状态（✓可写/…待补/○未定型）；
  **点击卡片定位到草稿对应节**（跳转手写区并选中节标题）；编辑功能保留在列表模式。
- **多作品对比**：作品库"对比"模式——选两篇作品，人类化指标并排
  （字数/句长标准差/段落变异系数/句首去重率/二元 TTR/反 AI 命中），
  Web `/api/works/compare`。
- 测试：api-smoke 增加 作品对比 断言；web 7 套 QA 全绿（含前端接线检查）；
  版本 0.48.0，引擎同步。

## 2026-08-11（v0.47：Web 人机交互 P1 收尾——并排伴随对话 + 伏笔回收时间线）

## 2026-08-11（v0.47：Web 人机交互 P1 收尾——并排伴随对话 + 伏笔回收时间线）

- **并排伴随模式（NovelCrafter split）**：进入写作/修订/审计/交付阶段自动开
  "并排"（聊天 54% + 手写区 46% 同时可见），边看 AI 写边亲手改；Esc 退出，
  用户手动关闭后不再自动开；顶栏"并排"按钮可随时切换。
- **伏笔回收时间线（P2 可视化第一项）**：审计页展示 已回收（✓ 于哪节）→
  未回收/悬空（○ 埋于哪节，提醒作者拍板），数据来自 consistency 后端。
- 测试：web 7 套 QA 全绿（含前端接线检查）；版本 0.47.0；引擎同步。

## 2026-08-11（v0.46：Web 人机交互 P1——改写候选卡 + 版本回滚 + 专注模式）

## 2026-08-11（v0.46：Web 人机交互 P1——改写候选卡 + 版本回滚 + 专注模式）

按 UX-PLAN 的 P1 路线继续：

- **改写候选卡（Sudowrite History 式）**：`point-edit.js` 新增 `rewriteVariants`
  （同一句生成 3 个方向不同的候选，不落盘）；`pointEdit` 支持直接应用指定候选
  （replacement 参数：跳过 LLM，只做定位校验+写回+风格吸收）；CLI `stylotrace rewrite`、
  Web `/api/rewrite`。前端选区工具栏点击动作 → 候选卡（选一个应用 / 换一个 / 取消）。
- **版本历史与回滚**：`pointEdit` 前自动快照（"每次 AI 改动都可回退"）；
  Web `/api/history` + `/api/rollback`；草稿区"版本"按钮 → 快照列表 → 一键回滚。
- **专注模式**：隐藏侧栏与伴随面板，只剩正文；Esc 退出（前端接线检查已适配）。
- 测试：新增 `agent/test/pointedit.test.mjs`（3 候选不落盘/应用候选写回+风格吸收/
  常规点改路径）；api-smoke 增加 候选改写/历史/回滚 5 项断言；
  agent 15 套 + web 7 套 QA 全绿；版本 0.46.0，引擎同步。

## 2026-08-11（v0.45：Web 人机交互 P0——选区 AI 工具栏 + 实时洞察 + 思想/风格可视化）

## 2026-08-11（v0.45：Web 人机交互 P0——选区 AI 工具栏 + 实时洞察 + 思想/风格可视化）

调研同行（Sudowrite Selection Menu、NovelCrafter 分屏伴随、Openwrite 选区操作与专注模式、
EmoArc 情绪可视化）后形成 docs/UX-PLAN.md 规划，实施第一批：

- **选区 AI 工具栏（手写区）**：选中文字 → 浮动出现 润色/扩写/缩写/更口语/更克制/节奏
  六个快捷动作，执行即写回草稿并吸收进风格档案（复用 point-edit，改动即风格教学）。
- **实时洞察卡（手写区顶部）**：字数/目标进度条 + 最近风格脉搏 + 节奏迷你曲线
  （张力/密度/情绪），输入即刷新。
- **思想脉络可视化（上下文面板）**：展示"AI 理解到的你的主张/前提/推理/来源"，
  随对话实时更新（v0.43 能力的落地）；风格进度升级为维度条 + Top 维度置信。
- 后端：`/api/context` 新增 pulses（风格脉搏列表）。
- 测试：api-smoke 增加 pulses 断言；web 7 套 QA 全绿（含前端接线静态检查）；
  版本 0.45.0，引擎同步。

## 2026-08-11（v0.44.1：提问顺序去硬编码——蓝图状态清单化 + RAG 知识背景 + LLM 自主决策）

## 2026-08-11（v0.44.1：提问顺序去硬编码——蓝图状态清单化 + RAG 知识背景 + LLM 自主决策）

用户要求"别做硬编码，用 RAG/LLM 优化"：上一版用排序映射钦定提问顺序，本质仍是代码
规定"先问什么"。本版把决策权还给 LLM：

- **删除排序映射**：genre.js 不再有 THINKING_FIRST 顺序表；字段声明顺序仅用于
  "清单展示与 LLM 停摆时的兜底"，不再驱动正常提问。
- **蓝图状态清单化（QUESTIONER_PROMPT）**：注入【蓝图状态】（每字段已确认✓/待补✗）
  与【我的建议下一步（可推翻）】，规则 10 改为"问什么由你判断"——LLM 结合用户刚
  说的话、思想脉络、知识背景自主决定下一个问题，可打破清单顺序接住用户；
  代码只在多问/重复/停摆时兜底。
- **RAG 知识背景注入**：追问设计师每轮拿到 unifiedBrief（用户知识库/阅读记录/素材
  检索），提问更贴用户（如用户读过《乡土中国》，可顺势问"文字下乡那段是否也影响
  你对烂梗的看法"）。
- 保留业务安全网：素材预算联动（字数确认后按新预算回补素材）、一次一问、低意愿逃生。
- 测试：thinking.test.mjs 新增"清单非命令 + 知识背景 + 可推翻建议 + 无本阶段最缺"
  断言；agent 14 套 + web 7 套 QA 全绿。

## 2026-08-11（v0.44：提问主次重排——先思想、后信息、最后规划，挖表达欲望）

承接 v0.43 思想脉络，用户进一步指出：澄清不能先用"写多长"这类基础问题打断表达欲，
要先问行文思路与关键信息，字数等规划参数最后问、并从内容反推。

- **蓝图字段主次（genre.js）**：统一排序映射——表达层（主题/想表达什么/核心立意/
  行文思路）→ 内容层（素材/论点/事项/人物）→ 规划层（读者/场合/依据）→
  可选维度（情感/结尾）→ 风格底稿 → **目标字数永远最后**（原先插在第二位）。
- **预算联动**：字数后问天然成立——素材门槛随最新 targetWords 重算，
  字数确认后若素材不足自动回补一次，防长文注水。
- **追问纪律（QUESTIONER_PROMPT 10.7）**：先挖"为什么现在想写/心里憋着的那句话/
  怎么展开"，再问关键信息，最后按内容反推字数（"写到多少字才讲得透××？"）；
  开局第一问绝不是"写多长"。
- **SKILL.md 澄清阶梯**同步新顺序。
- 测试：thinking.test.mjs 新增 3 组——蓝图字段主次、need 先问表达层、
  预算联动（字数最后问 + 确认后回补素材）。

## 2026-08-11（v0.43：思想脉络——追问从"搭结构"升级为"挖思想"）

## 2026-08-11（v0.43：思想脉络——追问从"搭结构"升级为"挖思想"）

手动测试反馈：AI 的问题能搭起大纲，但接不住用户抛出的理论/推理链
（"《乡土中国》讲语言简化是文化发展的结果，可以顺着推理"）——AI 只知道 1+1，
不懂为什么 1+1。新增思想脉络机制：

- **thinking.js（新）**：追踪用户发言里的"主张-前提-推理-理论来源"，LLM 优先做
  理论概括（THINKING_PROMPT），确定性只做信号识别与兜底；按来源/主张合并去重，
  最多保留 6 条，随 state 持久化。
- **追问设计师纪律（QUESTIONER_PROMPT）**：新增"思想层优先"——用户给出理论/因果
  推理/引用书籍时，下一问必须 ① 用原词复述主张 ② 顺着思路做一步概括推理
  （"你的意思是：烂梗不是没素质，而是语言简化走到极端后切断了共同意义——对吗"）
  ③ 追问根源（"是简化本身，还是共享语境的消失"），思想没挖透不跳去问素材/结构；
  "理论概括与思想共识"——思想脉络成形时整段回显请用户确认/纠正，纠正即最高价值。
- **大纲生长**：OUTLINE_PROMPT 注入思想内核区块，各节 thesis 从思想共识展开，
  不另起炉灶；clarify 每轮检测到思想信号才调 LLM（不空转），失败静默降级确定性。
- **可见性**：Web `/api/context` 返回 thinking 摘要；mock 增加思想层追问分支
  （仅本轮发言含推理标记时触发，不干扰既有澄清序列）。
- 测试：新增 `agent/test/thinking.test.mjs`（信号识别/合并去重/全链路思想追问）；
  api-smoke 增加 thinking 字段检查。

## 2026-08-11（v0.42.1：修复逐节写回的分节粘连——长文端到端测试抓到的真 bug）

## 2026-08-11（v0.42.1：修复逐节写回的分节粘连——长文端到端测试抓到的真 bug）

新增 `web/test/longform-qa.mjs`（12000 字小说全链路，接入 `web npm test`），
驱动真实 HTTP 处理器从澄清走到交付，验证卷级大纲贯穿全程。测试暴露并修复：

- **真 bug：逐节写作后节标题粘在上一节末尾**——导演每步只写一节，
  `existing.split(/\n(?=## )/)` 会吃掉 `##` 前的换行，直接 `join` 后
  "## 二、…" 被拼进第一节正文末尾，分节结构损坏（节奏曲线少节、伏笔校验错位、
  大纲面板错乱）。修复：`write.js` / `restyle.js` 写回前给每段补齐结尾换行
  （`joinParts` 安全网），并加"节标题独立成行 / 无粘连"回归断言。
- 长文澄清不再假死：素材门槛未满时同类问句 ≥4 次即低意愿逃生（真实产品中
  questioner LLM 会换问法，mock 固定问法由测试剧本模拟用户"你决定"）。
- 测试：longform-qa 15 项断言全过；版本同步 0.42.1；引擎同步后提交。

## 2026-08-11（v0.42：卷级大纲——长文结构分层，展示分组不碰写作真源）

补齐行业六尺子第①项"大纲层级"的长文部分：长文（≥6000 字或长篇/中篇/系列）由 LLM
在完整平铺的 sections 之外输出可选 parts 卷级分组（每卷 title + 节 heading 列表）。

- **LLM 优先、零硬编码**：parts 只是展示分组；sections 仍是写作与字数分配的唯一真源，
  write 链路零改动。代码只做规范化（`normalizeParts`）：空卷丢弃、错引用过滤、
  未分组节自动收尾，绝不删节改序。
- **导演/CLI**：`generateOutline` 输出 `outline.parts`，摘要体现"N 卷 · M 节"；
  director 的确认大纲视图携带 parts。
- **Web**：`/api/outline` 保存时接受并规范化 parts（沿用旧分组、按新节列表重挂）；
  `/api/context` 返回 parts；大纲面板按卷渲染分组标题（编辑索引仍扁平）。
- **测试**：live-outline 新增"卷级分组不改变写作真源"与 normalizeParts 容错断言；
  api-smoke 覆盖 parts 保存/返回/引用校验。
- 自检（用户要求）：agent 13 套 + web 6 套 QA 全绿；密钥扫描、循环依赖、语法检查、
  硬编码路径扫描全过；引擎同步后提交。

## 2026-08-11（v0.41：行业调研落地——节奏曲线 + 伏笔回收 + 隐式风格流水）

深度调研同行（马良写作 18 款横评的"六把尺子"、Sudowrite/NovelCrafter 等）与最新论文
（EMNLP 2025 Findings《Catch Me If You Can? Not Yet》——少量样本模仿个人风格显著失败；
ACL 2025 StyleVector——激活空间风格向量；GhostWriter——隐式学习 + 显式教学时刻），
形成 `docs/competition/BEYOND-PLAN.md` 超越方案，并落地差距最大的三项：

- **三线节奏曲线（对标行业"节奏分析"尺子）**：`style-pulse.js` 新增 `rhythmCurve`，
  按节输出 张力/信息密度/情绪强度/节奏变化 四条 0-100 曲线，落盘 `vault/curve.md`；
  新 CLI `stylotrace curve`、Web `/api/curve`、审计页迷你曲线。
- **伏笔记账 + 跨章回收校验（对标行业"一致性"尺子）**：新增 `consistency.js`——
  小说/推理每写一节自动记账伏笔（LLM 提炼，确定性兜底），全文完成后跨章校验回收；
  新 CLI `stylotrace consistency`、Web `/api/consistency`、交付消息带悬空伏笔提示；
  导演 revise 阶段自动触发，不打断流程。
- **隐式风格信号流水（对标 GhostWriter + EMNLP 2025 结论）**：`style.js` 新增
  `recordImplicitSignals`，每轮对话被动采集写 `vault/style-signals.jsonl/md`，
  压缩不丢风格；`stylotrace style --signals` 可回看，风格档案导出包含流水。
- **架构梳理（docs/ARCHITECTURE.md）**：全量梳理导演状态机、Agent 合作体系与
  "LLM 优先 / 代码安全网"边界，附新增模块入场检查；修复 revise 阶段
  `checkConsistency` 写盘后被旧 state 覆盖的一致性丢失 bug（校验后重读）。
- 测试：新增 `agent/test/consistency.test.mjs`（节奏曲线/伏笔记账去重/跨章回收/悬空标出/
  隐式信号 15 项断言）；web api-smoke 增加 curve/consistency 端点检查。
- 版本：agent/web 同步 0.41.0；引擎同步后一并提交。

## 2026-08-11（v0.40：最终大检查——风格差异验证 + 全问题复核 + 商业化检查）

新增 `web/test/final-qa.mjs`（接入 `web npm test`），并修复最后发现的问题：

- **风格差异验证（核心）**：同题（北大红楼散文）× 三套风格（克制留白 / 口语亲切 /
  豪迈大气）各跑完整流程，验证：写提示词互不相同、方向词/派生维度进入提示词、
  **成稿统计显著不同**（句长σ 3.7 / 8.3 / 5.0，字数 742 / 874 / 984）——风格注入
  真实改变了写法。
- **真 bug：扩写会冲掉风格**——写作者按风格写了，扩写提示词却不带风格方向，扩写
  回落到默认腔调把风格抹平。修复：`EXPAND_PROMPT` 注入【写作风格方向】与【写作风格
  档案】，扩写必须延续方向。
- **历史问题复核**（你在全程提过的）："好"不误判低意愿、大纲编辑不扰乱确认流程、
  读者群像在交付链路触发、一次一问、低意愿逃生、风格底稿记录、多模态/RAG/点改/回译、
  双端交付、导出格式、字数达标——final-qa 逐条抽查 + 既有 5 套 QA 覆盖，全部通过。
- **商业化检查**：`scan-secrets.sh` 无泄漏、无硬编码 API key（排除测试桩）、
  `install.sh --dry-run` 正常、agent/web 版本一致、README/CHANGELOG/LICENSE 齐备、
  风格/向量/侧写接口完整。
- mock 风格管线：写作者/扩写按方向词返回不同风格变体，风格提取按方向返回对应维度
  （无方向时中性，不污染默认会话）。
- 测试：agent 12 套件 + web 6 套 QA（冒烟/全功能/十种用户/红队/深度/最终）全绿。

## 2026-08-11（v0.39：深度 QA——双端全格式 + 字数达标保障）

新增 `web/test/deep-qa.mjs`（接入 `web npm test`）：Web 与 CLI 双端同跑论文全流程，
验证"产出格式完整、字数达标"，并做前端接线静态检查。发现并修复：

- **真 bug：红队修复把全文顶掉**——修复提示词要求"只输出改写后的片段"，但代码把整份
  draft.md 替换成模型返回，片段把成稿覆盖（成稿从 2200 字被压到 150 字）。修复：
  提示词改为"输出修改后的完整全文，保持篇幅与结构"；修复稿按全文写回。
- **字数达标保障升级**：扩写阈值 60% → 85%（提示词要求 ±15%），且扩写最多重试两轮；
  `quality.words` 按全文统计（导演逐节调用时不再只算单节）；字数未达标在交付消息
  明确警告（"说'再详细点'我会补齐"）。
- **大纲字数重标定**：LLM 大纲分配字数明显不足用户目标（如 2000 字只分 900）→ 按比例
  重标定各节字数，写作预算贴合用户需求。
- **前端接线检查**：视图/阶段注册表、面板页签、阶段条→视图映射、全部 `$()` 引用都有
  对应 id（HTML 或动态创建）——全部通过。
- 导出验证：md / docx（zip+document.xml）/ pptx（zip+slide）/ html / srt（时间轴）/
  pdf（reportlab 可用则 %PDF，缺依赖则优雅报错）。
- mock 长稿修复模拟"只改问题不删内容"，短稿返回结构完整的干净文本（过全部审计指标）。
- 测试：agent 12 套件 + web 冒烟 13 组 + 全功能 QA + 十种用户 QA + 红队 QA + 深度 QA
  全绿；Web/CLI 双端论文 1838/2000 字达标。

## 2026-08-11（v0.38：红队全流程压测——多文体 + 对抗输入，修复 6 类真问题）

新增 `web/test/redteam-qa.mjs`（接入 `web npm test`）：8 类文体（论文/公文/合同/新闻稿/
发言稿/小说/议论文/视频脚本）× 6 类对抗输入（超长/乱码/疯狂否定/数字滥用/跳跃回答/极短/
空输入），全部走真实 Web HTTP 处理器。压测暴露并修复：

- **文体误改**：答案里的"故事/通知"等词会把发言稿/议论文误识别成小说——文体只在开局
  识别一次，用户显式说"改成/换成"才允许切换。
- **风格"没有"死循环**：用户对风格题答"没有"不会被标记完成，反复追问——现在答"没有/
  不用/没写过"即标记风格底稿已问过。
- **可选维度"跳过"不生效**：角色/情感/结尾等可选字段说"跳过"后仍反复问——按文体蓝图
  判断可选性，跳过即标记完成（含 emotion/ending 存储键修正）。
- **LLM 反复问已满足维度（合同被反复问素材）**：新增软性拉回——LLM 问一个已满足/蓝图外
  维度而当前还有必填缺口时，拉回当前缺口（只保底、不硬编码问法）。
- **列表字段拆分**：公文/合同要点、论文论点一条消息含顿号/逗号多项 → 拆成多条入列，
  不再整条当一项导致"永远凑不够 N 条"。
- **gate 与文体蓝图错配（合同卡死）**：大纲生成门槛改为与澄清共用同一套文体蓝图
  （`requiredMissing`），合同要事项/主送、小说要情节、论文要论点×N；并修复大纲阶段
  "还差一点信息"问→答→重新生成死循环（先经澄清接住回答）。
- **情节字段丢失**：`classifyAnswer` 补 plot 识别，小说的情节回答不再误入素材。
- **列表重复提示推广**：素材/论点/事项重复提供时提示"已记过，换一条或说你决定"。
- e2e 改为"按问题动态作答的模拟用户"，不再把测试绑死在特定问序上。
- 测试：agent 12 套件 + web 冒烟 13 组 + 全功能 QA + 十种用户 QA + 红队 QA 全绿。

## 2026-08-10（v0.37：大纲回归"LLM 从对话总结的呈现物"，不再约束 LLM）

按"大纲不能限制 LLM，而是根据与用户的交流由 LLM 总结、呈现给用户"重构：

- **不再预造骨架**：实时大纲从空开始，不强制"开头/主体/结尾"；由 LLM 每轮根据对话
  用 `outlineUpdate` 自然总结（标题/各部分/关键点），右侧面板随对话成形。
- **不再缺口驱动提问**：删除确定性缺口问题与"下一问补第 N 节"机制——问题完全由 LLM
  从对话与用户意图生长；`outlineComplete` 由 LLM 判断（或用户拍板/低意愿）。
- **完成度不再当裁判**：确认点由 LLM 判定成形触发（或用户说"开始写作"）；删除
  完成度百分比、每节状态徽章、缺失标签、字数预算等机器展示，状态条只剩
  "讨论中/已成形/已确认"。
- **蓝图确认并入大纲确认**：移除与大纲确认重复的"整篇文章蓝图确认"环节，interview
  清单同步（大纲确认由 LLM 成形或用户确认判定）。
- 提示词明确"大纲是总结不是约束：不硬套固定节数、不为了凑结构问缺哪一节；
  素材不足不机械追问"。
- 保留的最后安全网（不框死 LLM、只防卡死）：一次一问 ≥3 问号/编号兜底、低意愿×2
  与"开始写作"逃生门、deferred 放行、重复素材提示、兜底问题全覆盖。
- 测试：live-outline 重写为 6 场景（空骨架/LLM 总结成形→确认/确认进写作/随时拍板/
  低意愿逃生/打磨回路）；e2e 与 Web 三套 QA 适配新流程，全绿。

## 2026-08-10（v0.36：去硬编码——回归"LLM 优先、代码只做安全网"）

针对"web 化后提问死板、硬编码约束挤掉模型判断"的架构级回调，原则对齐 Codex 的做法：
**模型负责灵活判断（系统提示词 + 上下文），代码只在极端情况兜底，不替模型做决定**。

- **缺口提问改回 LLM 优先**：大纲缺口的"问哪一节"仍由确定性状态机定位，但"怎么问"
  交回追问设计师（沿用用户原词、自然生长）；确定性缺口问题只做 LLM 停摆/多问/重复时
  的最后兜底。此前是直接返回模板问题，正是"死记硬背"的来源。
- **删除硬编码维度分类器**：不再用正则猜"两个问号是否跨维度"，一次一问以提示词为主
  （自主遵守），代码只保留 ≥3 问号 + 编号/另外/其次 的最后防线。
- **主题预填 LLM 优先**：首轮主题由主题提炼器自然语言理解（去话术、留用户说法），
  启发式正则只做 LLM 不可用时的兜底。
- **放弃追问不再塞占位素材**：用户明确结束（连续两次低意愿 / 说"开始写作"）时标记
  deferred，大纲按现有信息生成、缺口交给写作阶段标注补全；不再往素材里塞假占位。
- **素材不足不机械追问**：提示词明确"现有素材够我起笔，缺的写作时标注再补，继续推进"。
- 回归：agent 12 套件 + web 冒烟 13 组 + 全功能 QA + 十种用户 QA 全绿（确定性兜底路径
  在 mock 下照常被验证，真实 LLM 走灵活路径）。

## 2026-08-10（v0.35：十种用户说法全链路 QA + 体验硬化）

新增"按问题动态作答的模拟用户"测试（`web/test/persona-qa.mjs`），十种真实用户说法
全部跑通到交付，并修复测试暴露的产品缺陷：

- **首轮主题预填**：用户开局已说出主题（"写一篇关于××的…"）就不重复问"你想写什么"，
  直接清理话术前缀存入 `confirmed.topic`，从下一个缺失维度问起；不够明确的输入不预填。
- **打磨问题归类修复**：打磨问题文本含"大纲/结尾"字样，用户回答曾被误判成"大纲确认"，
  导致确认/打磨循环；`classifyAnswer` 现在优先识别打磨问题，`applyAnswer` 新增打磨分支
  （内容意见进修正档案，确认话术直接定稿）。
- **逃生门（防死循环）**：连续两次"你决定/可以了"无论核心字段是否齐都放行进大纲
  （缺失项用占位补齐，写作时再补）；明确说"开始写作"同样尊重用户拍板。
- **素材重复提示**：用户重复提供同一条素材时，下一问会提示"上一条已记过，换一条新的，
  或说你决定"，不再无感重复追问。
- **兜底问题全覆盖**：known/gap/method/limitation 等维度原来没有确定性兜底问题，
  LLM 停摆时会问无关维度造成死循环；现在每个维度都有"还差「×」——直接说或回跳过"，
  且"跳过"会标记可选维度为已完成。
- 测试：十种用户（详细/极简/不耐烦/健谈跑题/公文命令/学术/否定/口语/双语/情感倾诉）
  各 8 项断言全绿（完整交付/问题数 4–26/大纲≥3 节/成稿≥150 字/风格采集/记录完整/
  全程一次一问/首问符合预期）；`web npm test` = 冒烟 13 组 + 全功能 QA + 十种用户 QA。

## 2026-08-10（v0.34：Web 全功能 QA 与体验硬化）

用真实 HTTP 处理器 + mock LLM 做了多类真实用户的全功能测试，修复测试暴露的体验缺陷，
并把 QA 固化为回归测试：

- **修复：手动编辑大纲不再扰乱确认流程**（`/api/outline` 移除 `justRefined` 副作用），
  编辑即时生效，完成度判定自然决定下一问（缺口/确认），不再出现"确认题→打磨题"错乱。
- **修复：低意愿早退死循环**——用户连说两次"你决定"后，导演直接进大纲生成，
  不再被"大纲缺口"的 missingNeed 卡住反复追问。
- **修复：一次多问判定细化**——同维度复述（"写多长？大概多少字？"）不算多问；
  ≥3 个问号、跨维度双问、编号/另外/其次/同时 列举才判多问并退回单问句；
  去掉对"还有没有别的素材？"的误判。
- **修复：确定性兜底问题与上一问相同时追加变体**（"…？还有别的吗？"），避免用户
  觉得同一句又问一遍。
- **新增 `web/test/fullflow-qa.mjs` 综合 QA**：模拟三类真实用户（认真作答 / 不耐烦
  "你决定"×2 / 不给风格底稿），全链路校验：一次一问、无连续重复、问题总数 ≤22、
  大纲完成度推进到 100%、明确确认点、自动交付、成稿字数、审计指标、交付质量门
  （含自动回译校验）、风格采集（write/read）、对话记录完整性、md/docx/pptx 导出
  （docx 校验 word/document.xml、pptx 校验 slide，均验证 zip 完整性）、上传/RAG/点改/回译入口。
  接入 `web npm test`（api-smoke 13 组 + QA 全绿）。
- mock 澄清问题规范为单问句（模拟良好体验，不再产出"双问号"噪声）。

## 2026-08-10（v0.33：回译校验自动触发——agent 交付质量门 + Web 审计入口）

把 v0.32 的回译校验从"按需命令"升级为"默认自动触发"：

- **agent 自动触发**：交付前静默质量门新增内容保真检查——中译英→回译→信息点核对，
  结果写入 `state.quality.roundtrip`；`attention`（有丢失/漂移）时交付消息给出明确警告
  （"运行 stylotrace roundtrip 看明细"）。LLM 不可用自动降级，超 3000 字单批上限自动跳过，
  `STYLOTRACE_ROUNDTRIP=0` 可关闭；绝不阻塞交付。
- **Web 一键入口**：审计（report）页新增"内容保真 · 回译校验"区块——显示交付时自动
  校验的结果，另有「运行回译校验」按钮（`POST /api/roundtrip`）即时出报告：
  判定 + 信息点核对 + 风格对比；`/api/report` 同步返回已存结果。
- 配置：`STYLOTRACE_ROUNDTRIP` 开关（默认开）。
- 测试：api-smoke 扩至 13 组（含回译校验报告/核对/风格指标）；e2e 断言质量门真实写入
  `quality.roundtrip`；全套单测通过。

## 2026-08-10（v0.32：翻译/回译校验——翻译官 skill，按需启动）

把"翻译的本质是同一件事用两种语言说清楚"落地为按需启用的能力，用于内容保真审计与风格对比：

- **引擎 `agent/src/roundtrip.js`**：内容分析（提取专名/数字/论断/意象）→ 中译英直译 →
  忠实回译 → 信息点核对（保留/丢失/漂移，LLM 判定 + 确定性兜底）→ 风格对比
  （原文 vs 回译文：句长σ / 句首去重 / TTR / AI 套话，复用 humanMetrics 与风格向量）。
  任何一步 LLM 不可用都确定性兜底，绝不中断。
- **CLI `stylotrace roundtrip [文本|文件] [--file x.md]`**：文本直接传或读文件，缺省读工作区
  draft.md；`STYLOTRACE_DEBUG=1` 可看完整堆栈。修复 CLI 工作区约定冲突
  （文本在 positional[0] 时误把文本当工作区目录创建）。
- **独立 skill `skills/translator/`**（随 install.sh 安装到 `.codex/skills/translator`）：
  零依赖独立脚本 `scripts/roundtrip.mjs`（Node ≥18），离线 `--metrics-only` 可跑，
  未配 API Key 自动降级；SKILL.md 明确触发条件——用户要求翻译/回译/翻译后查信息丢失时才启动，
  不混入普通写作流程。
- 测试：`roundtrip.test.mjs` 3 场景（全链路/LLM 不可用兜底/读 draft.md），全套单测通过；
  skill 通过 quick_validate。

## 2026-08-10（v0.31：Web 补齐三能力——RAG 闭环 / 多模态上传 / 句子级点改）

Web 端补齐与 agent+codex 的差距，核心写作链路仍与 CLI/agent 同一引擎：

- **联网 RAG 闭环**：新增 `GET /api/rag/needs`（待检索队列）、`POST /api/rag/search`
  （直连检索 + 自动回灌）、`POST /api/rag/ingest`（粘贴资料/结构化 results 回灌）。
  回灌即写进素材并标记 `ragIngestedAt`，导演在下一轮自动重写缺口节。
- **联网检索开箱即用**：引擎 `searchOnline` 支持主流服务商——`STYLOTRACE_SEARCH_PROVIDER=tavily|serper`
  + `STYLOTRACE_SEARCH_API_KEY`（或沿用自建 `STYLOTRACE_RAG_ENDPOINT`）；未配置时优雅降级
  （提示手动粘贴回灌），绝不阻塞写作。
- **多模态上传**：`POST /api/upload`（base64，≤20MB）→ 落会话 uploads → `extractInput`
  提取 docx/xlsx/md/txt/图片/音频 → 直接进素材；前端"素材上传"按钮即传即用。
- **句子级点改**：`POST /api/point-edit`（选中原文 + 修改指令）→ 引擎 `pointEdit`
  只改目标区间（并发守卫，冲突即退让）→ 吸收进风格档案与风格向量；前端写作面板
  「AI 改写选中」：自动先保存草稿 → 改写 → 刷新草稿并提示风格档案更新。
- **上下文面板**：新增"素材上传"与"资料检索（RAG）"区块，显示待检索数 / 检索配置状态；
  `/api/context` 增加 `rag` 状态（缓存/待办/直连/服务商）。
- 测试：Web 冒烟扩展至 12 组（上传提取、RAG 回灌、点改生效、文件改写、面板正常），全套通过。

## 2026-08-10（v0.30：大纲完成度确定性结算 + 缺口驱动提问 + 大纲可视化）

针对"实时大纲仍不实时、完成度靠模型口头宣称、大纲成了唯一裁判"的三层升级：

- **完成度确定性结算（不靠 LLM 宣称"完整"）**：新增 `agent/src/outline-state.js`，
  每节状态机 `idea → needs → ready`，按可验证清单结算（功能定位/要点/素材/核心句），
  加权百分制 + 全局缺口（≥3 节/开头节/收束节/核心立意/字数分配）+ 字数覆盖。
  完成度挂在大纲上，贯穿澄清、正式大纲生成、Web 面板三处；`complete = ≥80% 且无待补节`。
- **大纲缺口驱动提问**：核心信息齐、大纲未满时，问题由"最早未就绪的一节"决定
  （如"第 2 节「主体」还缺素材"），回答**直接落进对应节**，面板每轮真实长大；
  用户答"没有/不知道"→ 该项记入放弃，换下一缺口，绝不车轱辘；
  同一缺口连续两次未补 → 自动跳过；确认题上"再打磨一下"有确定性打磨问题兜底。
- **大纲只是视图，不是唯一真源**：提示词明确"立意/素材/风格/知识库才是写作真源，
  大纲只是把已确认信息组织成结构呈现"；用户在任意一轮说"大纲完成，开始写作"都可直接拍板
  （拍板话术不会被误收进素材/要点）。
- **前端可视化**：右侧大纲面板顶部完成度进度条 + 百分比 + 可写/待补/未定型计数 +
  字数预算；每节状态徽章（✓可写/…待补/○未定型）与缺失标签；"下一问将补"提示；
  每 4 秒自动轮询，讨论与写作中面板自己动；正式大纲页也显示完成度与每节状态。
- **修复的隐藏缺陷**：`advanceToOutline` 用旧 state 写盘会冲掉刚生成的
  `liveOutline.progress`（改为生成后重读再写）；`sanitizeOutlineSections` 会冲掉
  用户"放弃项"导致死循环（补保留）；缺口问题答案归类统一进素材桶，避免误入公文 items；
  LLM 每轮 `outlineUpdate` 不再冲掉用户已填的要点/素材（同节合并保留）。
- 测试：`live-outline.test.mjs` 扩展至 8 场景（完成度结算/缺口落节/放弃换题/确认点/
  随时拍板/再打磨回路）；e2e 模拟用户补齐缺口后走通 澄清→缺口×3→确认→大纲→写作→交付。

## 2026-08-10（v0.29：实时大纲驱动——大纲贯穿全程，完成即进入写作）

针对反馈"实时大纲不实时、没有确认方式、问题没由大纲决定"的架构级修复：

- **大纲成为讨论的可见核心**：澄清从第一轮起就有一份实时大纲（种子骨架：开头/主体/结尾），
  每轮回答后 LLM 输出 `outlineUpdate`（完整节列表增量合并），右侧面板逐节生长，讨论推进看得见；
  LLM 不可用时确定性骨架兜底，绝不空白。
- **提问由大纲状态决定**：追问设计师读【当前实时大纲】，问题专攻缺口——缺开头/主体/结尾、
  某节缺素材/缺要点/缺结尾姿态就问那一块；大纲结构完整（≥3 节）时 `outlineComplete` 信号触发。
- **明确的"开始写作"确认点**：大纲成形后对话出现确认题「确认这份大纲，开始写作吗？」，
  右侧面板同步出现大按钮「大纲完成，开始写作」；说"再打磨一下"→ 修正入档 + 补一个打磨问题，
  打磨后再次回到确认题——想磨多久磨多久，确认后自然进入大纲生成与写作。
- **大纲深度编辑器**：标题/节标题/功能/字数/要点全部可改，支持新增一节、删除、↑↓ 排序，
  「保存大纲」直接落库（`/api/outline`），下一问按你的新大纲继续；写作阶段大纲转只读参考。
- 大纲生成以实时大纲为起点（保留结构要点，补字数/论点/素材分配，不推倒重来）。
- 右侧面板状态条（讨论中 · 大纲可确认 / ✅ 大纲已确认 · 写作进度）；CLI 同步打印实时大纲。
- 新增 `agent/test/live-outline.test.mjs`（种子骨架/确认点/确认进写作/再打磨回路），
  全套单测 + Web 冒烟全绿。

## 2026-08-10（v0.28.1：修复首页开始写作无反馈 + 右侧面板遮挡对话）

- **根因 1（无跳转反馈）**：首页点「开始写作」后，加载中/报错信息都追加在隐藏的聊天区，
  真实 LLM 多轮调用较慢时首页看起来"点了没反应"。修复：点击后**立即切换到会话视图**，
  先显示"正在理解你的想法…"，成功后再渲染首问；失败时错误直接显示在可见对话里。
- **根因 2（遮挡）**：v0.28 重写面板时丢失了 `body.panel-open` 类切换，主区让位 CSS
  从未生效，右侧面板以固定层直接盖住聊天区。修复：补回 panel-open 切换，
  面板 380→360px，>1280px 与 861–1280px 两档让位，≤860px 恢复悬浮层。
- 按钮导航全量审计：开始写作/种子/侧栏/大纲确认与修改/成稿/审计/阶段条/面板页签均正确。
- 对话区微调：气泡最大宽度 78%→82%、聊天区高度 56vh→54vh，拥挤感减轻。

## 2026-08-10（v0.28：伴随式写作工作台——右侧实时大纲 + 手写草稿区）

- **右侧常驻工作台（三页签）**：大纲 / 写作 / 上下文，进入会话即展开，不再只是可折叠的悬浮层；
  主区自动让位（宽屏下右侧 380px 面板），每个页签都完整可用。
- **实时大纲页签**：澄清阶段显示整篇蓝图（可折叠）+ 确认清单（✓/…）；大纲生成后
  **逐节实时呈现**（标题/功能/字数/论点/要点），可**手改节标题、删节、新增一节**，
  一键「提交大纲修改」→ 作为修改意见发给 Stylotrace 重新生成；下方常驻「给 Stylotrace 的建议」输入框，
  任何阶段都能直接提建议（写作进行中会提示先等写完）。
- **写作页签**：左侧对话、右侧手写——写作进度条（x/y 节）+ 可编辑草稿区（字数实时统计、
  保存写回 draft.md、查看成稿、导出 md/docx/pptx）+ 大纲只读参考。
- **上下文页签**：我的理解、素材、风格进度、回答层次 L0–L5（原 Agent 面板内容保留）。
- 页签随阶段自动切换（澄清/大纲 → 大纲页，写作/交付 → 写作页），用户手动切换后不被打断。
- `/api/context` 返回完整大纲节列表与写作进度；冒烟测试补充断言，全套单测 + Web 冒烟全绿。

## 2026-08-10（v0.27.1：修复"帮我查一查被跳过"——显式检索 + 引用式知识库收录）

**根因**（用户真实反馈："对……语言在变得匮乏。我曾经在乡土中国中听到……你可以帮我查一查吗"被直接跳过）：
- `captureKbMentions` 只收录"确认读过/喜欢"的《书名号》书 → 无书名号、无确认信号的引用式提及
  （"在乡土中国中听到一段论述"）完全没进知识库；
- RAG 只在"论文/报告/新闻稿且素材不足"时被动触发 → 散文场景下用户主动要求查证被静默跳过。

**修复**：
- **知识库**：新增"引用式收录"——无书名号的引用语境（"在××中听到/论述/提到…"）与
  书名号引用语境（"在《××》里有段论述…"）都会低置信（0.6）收录，source 标记 `user-referenced`，
  可删除、可提升；"明确读过"仍走 `user-confirmed`。误报防护：站/坐/待等动词后的"在…里"不匹配，
  "这/那"开头的泛指（"那间教室"）不收录。
- **RAG**：新增显式检索通路 `explicitSearchSuggestion`——检测"帮我查一查/查一下/搜一下/找一下/查证"
  等意图（"检查一下/审查"等不误触发），立即：配置了 `STYLOTRACE_RAG_ENDPOINT` 则直连检索并回灌素材
  与知识库（web-rag 低置信书目）；未配置则排队宿主代检（requests.jsonl，`stylotrace rag ingest` 回灌）。
  同查询去重，且会把提示语返回给用户（"已排队检索「乡土中国 语言变简单」…"）。
- **全链路**：CLI 与 Web 的澄清响应新增 `searchSuggestion` 字段并展示（Web 带 🔎 标识）。
- 新增 `agent/test/explicit-search.test.mjs`（6 组：引用式收录/确认信号/意图触发/全链路/去重），
  全套单测 + Web 冒烟全绿。

## 2026-08-10（v0.27：Web 端大改——从演示页升级为写作工作台）

- **首页 Dashboard**：项目/作品/草稿/知识条目统计卡 + 最近项目一键续写，从"输入框"变成"工作台"。
- **三栏工作台**：左侧导航 / 主区视图 / 右侧 Agent 上下文面板（可折叠）。
- **Agent 上下文面板**（`/api/context` 实时数据）：我的理解（intent 摘要/核心诉求/风险）、
  确认清单（✓/…）、整篇蓝图、素材、风格进度、回答层次 L0–L5 分布——用户全程看得见 AI 在想什么。
- **风格肖像大改**：Canvas 雷达图（write 14 维置信度多边形）+ 维度卡片 + 四层向量 + 采集进度。
- **作品库/知识库**：标题搜索 + 分类/类型筛选；作品库补字数统计。
- **成稿**：实时字数显示，编辑时同步更新；预览/编辑/导出（md/docx/pptx）不变。
- 后端新增 `/api/context`、`/api/overview`；冒烟测试扩展覆盖。

## 2026-08-10（v0.26.1：对话引导参考落地——把用户推向 L2–L5 级回答）

- **参考文档入库**：`docs/DIALOGUE-GUIDE.md`——基于散文《再见》与中篇《差生》两场真实会话，
  归纳优质回答的六维判定、L0–L5 分级、10 条引导技术、13 条原则与反面清单。
- **追问设计师强化**（prompts.js）：每个问题默认带"我的建议"＋2–3 个真实方向选项并附理由；
  从用户原话里挑最亮的画面/悖论追问；用户否定时走"反向引导"（先复述不要什么，再问要什么）；
  小问题与大问题交替；固定台词/结局一字保留；蓝图回显引导全篇级校正。
- **澄清自检**（clarify.js）：每轮回答按 L0–L5 确定性分级并记入 context 日志（内置，不打扰用户），
  供实验数据与复盘；新增可见确认清单（✓/…、进度 x/y），CLI 与 Web 同步展示。
- **写作规则补充**（WRITE_PROMPT）：反转只能回收已有伏笔、禁止生造巧合；
  第一人称对话用裸对话＋动作＋注意力漂移，少"他说/我说"标签，书面应答词按作者习惯转口语。
- 新增 `agent/test/guide.test.mjs`（L 级分级 10 例 + 清单状态），全套单测与 Web 冒烟全绿。

## 2026-08-10（v0.26：意图理解层接线 + Web 工作台深度优化）

### Agent：解决"主次不清、扣细节、无法达成共识"
- **意图理解层（intent.js）正式接线**：每轮澄清自动提炼「我的理解 / 核心诉求 / 风险」，
  注入追问设计师（先对齐理解再问）；澄清收尾再刷新一次，让大纲生成的起点是"达成共识后的理解"。
- **追问硬规则**：只问"会改变文章走向"的高价值全局问题（主题/目的/读者/立意/结构/风格方向）；
  措辞、小场景、格式等细节一律留给写作后修改，杜绝扣细枝末节。
- **两道确定性护栏**：① 连续两次「没有更多/你决定/可以了」且核心字段已齐 → 直接进大纲，
  不再让 LLM 误判"继续吧=还没问完"；② 检测到与上一问同义/互相包含 → 自动换一个未确认维度问，
  绝不车轱辘式重复提问。
- **对话回复误判文体修复**：用户说"你决定/继续吧"这类口语回复会被 `detectGenre`
  的子串匹配误判成公文文种「决定」，从而把澄清带偏到"主送对象"——已加回复护栏
  （口语回复不触发文体识别；"写一份关于××的决定"仍正常识别）。

### Web：从单会话演示升级为写作工作台
- **多会话持久化**：会话存 `web-data/sessions/`（标题/分类/状态/时间），可列表、续写、改名、删除；
  刷新页面后对话记录完整恢复（transcript 重放）。
- **作品库**：跨会话聚合 `vault/library`，按文体分类展示；作品可点开阅读，导出 md/docx。
- **风格肖像可视化**：write 14 维 + read 7 维置信度条、四层复合风格向量、困惑度签名、
  采集进度一目了然；人物侧写（persona）按叙述视角/词汇/句式/情感等卡片展示。
- **知识库可视化**：条目卡片（类型/标签/来源/置信度/使用次数），可删除，只读不硬塞。
- **成稿编辑与导出**：预览/编辑切换 + 保存回写 draft.md；导出 md / docx / pptx
  （python-docx / python-pptx 可用时；pptx 按大纲自动分页）。
- 暖白主题保留，新增左侧工作台导航，所有视图切换沿用统一 viewIn 标准。

## 2026-08-10（Web 前端修复：静态资源 404 根因）

- **根因**：HTML 引用 `/assets/app.css`、`/assets/app.js`，但文件实际在 `public/` 根下，
  服务端静态路由映射到 `public/assets/` → 浏览器请求 CSS/JS 全部 404，
  页面一直以无样式"CLI 式"渲染（此 bug 自第一版起存在）。
- **修复**：样式与脚本移入 `public/assets/`，与 HTML 引用及服务端路由对齐；
  路径解析与元素 id 一致性均已验证。

## 2026-08-10（Web 前端改版：暖白亲和主题）

- **配色重设计**：暗夜书斋 → 暖白书房——米白底 / 暖棕文本 / 陶土橙点缀 / 衬线标题，
  更亲和明亮，适合展示与阅读；纸纹噪点改为浅棕细点，保持质感不抢戏。
- 设计系统结构不变（8px 网格、圆角层级、统一 viewIn 切换动画、阶段条状态规范），
  仅替换色板与组件在浅色下的表现（聊天气泡/大纲卡片/书页/审计卡片全部适配）。

## 2026-08-10（Web 演示版：把写作流程搬进浏览器）

- **`web/` 单会话演示版**：零依赖 Node HTTP 服务（`server.mjs`）把 Stylotrace 导演状态机包成
  REST API（start / step / draft），聊天式前端（深色侧栏 + 阶段进度 + 大纲卡片 + 成稿展示）
  完整呈现 澄清→大纲→写作→审计→交付。
- **离线 mock 模式**：`npm run mock`（`STYLOTRACE_MOCK_LLM=1`）无需密钥即可本地跑通全流程，
  适合先看效果与 CI 验证；真实模式 `npm start` 用 `STYLOTRACE_LLM_API_KEY`。
- **部署指南**：`docs/DEPLOY.md`——平台对比（Railway 最合适 / Render 备选 / Vercel 不推荐 /
  Cloudflare Tunnel 应急）+ Railway/Render 一键部署步骤 + 安全限制说明。

## 2026-08-10（产品展示网站）

- **GitHub Pages 静态展示站**（`site/index.html`，零依赖单页）：产品叙事 + 三大理念 + 能力 +
  工作流 + 研究/实验 + 安装；`.github/workflows/pages.yml` 推送即部署，
  访问 https://zhangyoufu-123.github.io/stylotrace/ （需在仓库 Settings → Pages 选择 GitHub Actions 源）。

## 0.24.0 (2026-08-10)

### Added（实验与数据采集引擎：把研究论文的实验设计落成工具）

- **`stylotrace experiment` 五个子命令**：
  - `metrics <file>`：人类化指标（句长标准差/段落变异系数/句首去重率/二元 TTR/困惑度签名/黑名单/重复比喻/句式复用），复用红队审计 + L3 签名；
  - `collect [工作区]`：采集作者语料包（旧稿样本/修改记录/对话话语/知识库/写作库/风格向量摘要）→ `vault/experiments/corpus.json`；
  - `run --topic "题目" [--authors "名=文件;名2=文件2"]`：对照实验——每位作者跑通用 LLM baseline vs 风格注入 variant，
    输出指标对比表 + 随机顺序 A/B 盲评对（`vault/experiments/run-*/`：results.json / blind.json / report.md）；
  - `ablation --topic ... --author 样本`：消融实验——依次关闭 styleShot/persona/knowledge/styleAdapter，量化各模块边际贡献；
  - `survey [--out file]`：盲评 + 用户体验问卷模板（理解度/掌控感/满意度/AI 味/澄清轮数/修改次数/耗时）。
- **降级原则**：无 LLM 密钥时对照/消融明确提示跳过，指标/采集/问卷等确定性功能照常可用。
- **盲评与汇总闭环**：`experiment blind <run目录>` 把盲评对导出成一页问卷（可直接分发）；
  `experiment summarize <run目录> [--answers]` 输出论文表格（客观指标 + 盲评选择率 + 二项检验 z 值）。
- **执行手册**：`docs/experiments/EXECUTION-GUIDE.md`（伦理声明/样本规范/三步实验流程/数据回填/时间建议）；
  研究论文新增 5.4"结果（待回填）"与 5.5"讨论要点"占位章节。
- **测试**：`agent/test/experiment.test.mjs`（18 项，离线 mock LLM）；全套 355 项断言全绿。

## 2026-08-10（比赛成果 · 研究论文）

- **新增 [docs/competition/RESEARCH-PAPER.md](docs/competition/RESEARCH-PAPER.md)**：
  正式研究论文《Stylotrace：以"作者建模"为中心的深度协作写作 Agent》——
  形式化"风格 = 个体表达对完美表达的结构化偏离"的差异弥补模型、四层风格表征、
  双风格分离原则、PKB 三纪律、认知写作流程实例化；并给出对照/消融/用户盲评
  三个实验的完整设计（待采集数据后补充结果章节）。

## 2026-08-10（发布版 0.23.0 · 比赛材料）

- **版本对齐**：package.json / MCP serverInfo / CLI help 统一到 0.23.0。
- **比赛材料**：新增 [docs/competition/PRODUCT-README.md](docs/competition/PRODUCT-README.md)
  （面向评审/用户的产品 README）与 [docs/competition/COMPETITION.md](docs/competition/COMPETITION.md)
  （论文式技术报告：摘要/相关工作/系统设计/核心方法/评估/参考文献）；
  根 README 增加产品总览与比赛文档入口。

## 0.23.0 (2026-08-10)

### Added（全盘优化：圣经 · 复阅循环 · 迁移 · 护栏 · 场景扩展）

- **文章圣经**：长文/小说交付自动沉淀跨篇一致性文档（世界观/角色/时间线/伏笔/文风/连贯性），
  `stylotrace bible list|view|save|distill`；续写时读取注入。
- **复阅-修订循环**（Flower & Hayes）：初稿完成后、红队前全文复查（偏题/衔接/素材用足/字数），
  P0 自动局部修订一轮（长文 ≥3 节、最多 1 轮、静默）。
- **多轮数据补给上限**：回灌重写后仍缺素材 → 再排队一轮（最多 2 轮），满上限交付带警告。
- **知识库迁移**：`stylotrace knowledge export|import`（按标题去重合并、含提问记录、隐私提示）。
- **风格过拟合护栏**：侧写注入提示"习惯而非牢笼，可突破"；提示词动态预算裁剪
  （超预算优先保风格适配卡，裁侧写/统一素材）。
- **修辞同一性**：侧写新增"与读者建立共鸣的方式"维度，并映射回风格向量。
- **情绪曲线量化**：`stylotrace emotion` 按节输出强度与主导情绪（平静/喜悦/哀伤/愤怒/张力）。
- **场景扩展**：文体库新增投标书/申报书；改写矩阵新增 `classical`（古文风）、`desensitize`（脱敏）。
- **联网内容分级**：网络来源书目入知识库默认置信度 0.5（⚠待核实），注入标注"联网资料·待核实"。
- **测试**：`agent/test/v23.test.mjs`（19 项）；全套 337 项断言全绿。

## 0.22.0 (2026-08-10)

### Added（人物风格肖像 · 内置库 RAG 化 · 去硬编码）

- **人物风格肖像（侧写）**：从个人知识库 + 个人写作库 + 修改记录 + 风格档案 + 复合向量，
  生成可查询的写作人格肖像（`vault/persona.md`：叙述视角/词汇/句式/情感/价值观/套路与盲区/引用习惯）；
  侧写注入大纲与写作（最高优先级），并映射回风格向量（`kind: persona`）。
  `stylotrace persona`（查询）/ `--refresh`（重新生成并映射）。
- **内置库 RAG 化（不硬编码、联网优先）**：思想库（荐书）与资产库（文法/诗词/论证）改为
  **联网检索优先、内置 JSON 离线兜底**——未命中自动排队宿主代检（thought-search / asset-search），
  `stylotrace rag ingest-assets <results.json>` 回灌 `vault/asset-cache.json`；
  回灌结果中的《书名》自动入个人知识库（source: web-rag，数据互通）；
  `unifiedBrief`/荐书联想缓存优先、内置库兜底；联网荐书可直接展示（`webRecommendation`）。
- **灵活化**：保留确定性兜底（LLM 不可用时流程不崩），但默认路径是"知识库/检索/侧写"，不再只靠写死的列表。
- **测试**：`agent/test/persona-rag.test.mjs`（18 项）；全套 318 项断言全绿。

## 0.21.0 (2026-08-10)

### Added（学术论证链 · 荐书联想 · 角色预演 · 统一素材体系）

- **学术实质能力（不依赖外部学术 agent）**：学术论证链 known→gap→tension→insight→method→
  evidence→limitation（参考 AgenTex / BuildIntroArgumentChain）；澄清可补问缺口/方法/局限；
  大纲与写作按论证链位置推进；学术表达规范注入（限定词/让步反驳/证据纪律）；
  `stylotrace academic` 查看论证链 + 成稿完备性扫描（claim/evidence/warrant，结论节加 limitation）。
- **荐书联想**：内置思想库（28 部经典书/理论，`templates/thought-library.json`），按用户主题
  匹配相近作品，用简明语言说明"理论是什么、为什么可以用"；只问一次、可拒绝；
  确认后收入个人知识库；已收录的不重复推荐、相近条目互链。`stylotrace recommend`。
- **角色预演（小说/推理）**：持久角色档案（背景/愿望/恐惧/秘密/说话方式/情绪/记忆，
  `vault/characters/`）；写作前按"理想-现实张力"让 LLM 以角色第一人称预测
  心里话/会说的话/具体动作/情绪/下一步（参考 MATE/DiriGent）；预演注入本节写作；
  情绪记忆回写保持连续；无 LLM 确定性兜底。`stylotrace character list|add|view|remove|simulate`。
- **统一素材体系**：`unifiedBrief` = 个人知识库 + 检索回灌来源 + 内置写作资产
  （文法连接/诗词典故/论证骨架，`templates/asset-library.json`，确定性防幻觉）；
  大纲/写作联合注入、限量轮换；各库数据互通。
- **测试**：`agent/test/asset-academic-character.test.mjs`（22 项）；全套 300 项断言全绿。

## 0.20.0 (2026-08-09)

### Added（数据回灌闭环 + 自动参考文献）

- **回灌后自动续写**：检索结果回灌晚于最后一次写作、且稿中仍有"【素材不足：还需要××】"的节时，
  导演在交付态自动重写缺口节（用新素材），再重新反 AI 审计后才交付——用户/宿主回灌后
  只需再调用一次 `agent_step`（或说"继续"），无需手动重跑。
- **自动参考文献草稿**：学术论文交付时，从检索回灌来源（rag-cache）自动生成 `references.md`
  （GB/T 7714 / APA），并在交付消息提示；`stylotrace citations --auto` 可手动触发。
- **时序记录**：`state.ragIngestedAt`（回灌）与 `state.lastWriteAt`（写作）用于精确判断
  "数据晚于写作"这一自动续写条件；退让协议不变（外部改过的稿仍不覆盖，仅报告）。
- **测试**：RAG 单测新增自动参考文献与回灌时间戳（19 项）；e2e 新增"回灌→自动重写缺口节
  →重新审计"全闭环（共 234 项断言）。

## 0.19.0 (2026-08-09)

### Added（主导式协作 + 实时取数 + 场景全打通）

- **主导式协作协议**：写作生态位内 Stylotrace 自主决策（agent_step 状态机），宿主与
  学术/数据分析 agent 作为执行与数据供给方；生态位外完全让位。`probe` 输出细分入口
  （academic/official/creative/style/outline/point-edit）与启动建议（agent/point-edit/none）。
- **实时取数三通道**：澄清（clarify-data）、大纲缺素材节（outline-gap）、写作"素材不足"
  标注（write-gap）自动排队检索；`stylotrace rag needs` / MCP `data_needs` 查看待办，
  `rag ingest` 回灌进素材并标记完成；请求去重、不阻塞流程。
- **学术论文闭环**：素材缺口检测（论文/报告/新闻稿必须有可查证的文献/数据）、
  交付时自动检测《引文》并提示生成 GB/T 7714 参考文献；导出走 `export --academic`。
- **MCP 新增 data_needs**（37 个工具）；导演 working 消息携带已排队检索提示。
- **测试**：`agent/test/rag.test.mjs`（16 项实时取数单测）；e2e 增加学术澄清自动排队
  → rag needs → ingest 回灌 → 待办完成全闭环（共 231 项断言）。

## 0.18.0 (2026-08-09)

### Added（个人知识库 PKB：把"读过什么 + 经历"沉淀成用户的独有资产）

- **归纳式采集**：澄清时用户提到《书名》→ 只问一次"如果《X》是你读过/喜欢的作品，告诉我一声"；
  答"读过/喜欢"自动收录，答"没读过"记已问过不再追问（`asked.jsonl` 去重）。
- **地点与构想**："去过/参观过 ×" 收录为 `place`；主题明确且库中无相关时，每会话最多一次
  泛问"这个话题你读过什么书、或去过相关的地方吗？"。
- **人可读存储**：`vault/knowledge/<id>.md`（JSON 头 + Markdown 笔记，0600），
  可阅读、可编辑、可带走；CLI 管理 `stylotrace knowledge list|search|view|add|remove`。
- **轮换注入**：大纲与每节写作按主题 BM25 检索注入 `【作者知识库·辅助参考】`，
  未用条目 +0.2 / 用过的按次数递减（封顶 -0.45），绝不反复引用同一本。
- **测试**：`agent/test/knowledge.test.mjs`（25 项：收录/去重/匹配/轮换/提问去重/归纳一问）；
  `npm test` = knowledge + e2e 全链路。
- 设计参考：MemGPT 分层记忆 / Alexandria 来源可引 / memories-off 代码化管理 / read-aware 引导访谈
  → `skills/stylotrace/references/knowledge.md`

## 2026-08-09（产品方向收敛）

- **放弃应用路线**：删除独立 Web 应用（stylotrace-app）与桌面 IDE（agent-studio）及全部应用产物/文档；
  本仓库（引擎 / CLI / skill / MCP）即产品，专注把 agent 做深做透并开源分发。

## 2026-08-07（仓库结构）

- **Web 前端从本仓库移除**：拆分为独立应用仓库 `stylotrace-app`（`../stylotrace-app`，面向非技术用户，
  自带引擎快照、零依赖、可 Docker 部署）。本仓库只保留引擎 / CLI / skill / MCP，不再含任何 Web 代码。
  引擎同步：`bash ../stylotrace-app/scripts/sync-engine.sh`（单一事实源仍是本仓库 `agent/`）。

## 0.17.0 (2026-08-07)

### Added（四层复合风格向量：把"风格 = 不规范表达与完美表达的差异"从理论落成实时向量）

- **L1 连续向量**：作者语料 embedding（默认稀疏字符二元组，零依赖；`STYLOTRACE_EMBED_BASE_URL/API_KEY/MODEL`
  可升级真实 embedding）相对基线语料的方向差，EMA 增量更新（`STYLOTRACE_STYLE_EMA` 调跟手度，
  `STYLOTRACE_BASELINE_TEXT` 提供通用基线）。
- **L2 动态稀疏维度**：基础 write/read 高置信轴 + 意象子维（联想库/注意力焦点）+ 偏好轴（修改意图归类）
  + 素材维（反复实词自动衍生）。权重 × 新鲜度衰减，限量注入提示词。
- **L3 困惑度签名**：确定性代理指标（少见二元组占比 + 低重复率 + 句长方差）累计作者 min/mean/max；
  红队审计对照本文——surprisal 明显低于作者基线 → 标记"比本人更顺"的 AI 平滑痕迹（软提示，不计硬失败）。
  `STYLOTRACE_PERPLEXITY_ENDPOINT` 可换真实端点。
- **L4 偏好对**：point-edit / 修改建议 = (原文, 改后, 意图) 最高权重风格证据，落 preferencePairs 并同步偏好轴。
- **混合检索**：风格记忆检索升级为 BM25 + 向量余弦加权（相关度 0.42 / 向量 0.26 / 时间衰减 0.18 / 重要性 0.14），
  无旧稿时也能注入实时向量维度与人类化签名。
- **全链路每轮刷新**：澄清/大纲/写作/改写/定点修改/风格方向/修改建议 7 个触点自动刷新
  `vault/style-vector.json`；新 CLI `stylotrace style-vector [--refresh]` 查看/重算。
- 压缩守卫 `refreshFingerprint` 纳入风格向量摘要（动态维度 + 困惑度签名 + 偏好对计数），
  压缩时风格不被遗忘。

### Changed

- 版本 0.16.0 → 0.17.0（CLI HELP / package.json / 文档同步）。

### Added（一键安装与三处自更新）

- `install.sh` 升级为**一次命令装/更新三个安装点**：`--all` = 全局 skill
  （`~/.codex/skills/stylotrace`）+ 当前项目 skill + 开发镜像（默认 `~/stylotrace`）；
  `--update` 先 `git pull` 再同步；`--dry-run` 全流程预览。
- 镜像同步是**选择性同步**（agent/skills/scripts/examples/extras/.github/.claude-plugin/
  .codex-plugin + 根文档），保留你的 `.git`、`node_modules`、`.env.local`，不整树 --delete。
- skill 内置自更新器 `skills/stylotrace/scripts/update.sh`：安装后任意位置一条命令
  `bash ~/.codex/skills/stylotrace/scripts/update.sh [项目目录]` 即可刷新三处。

### Added（篇幅预算：根治长文注水）

- 新增 `budget.js`：目标字数 → 节数（每节 ~360 字）/ 每节字数 / 素材下限（每 ~350 字 1 条）/
  论点下限（议论文/报告每 ~900 字 1 个）/ 事项下限（公文系每 ~600 字 1 条）。
- 澄清新增**目标字数必问维度**（中/阿数字解析：`三千字`/`3000字`/`大约一千字`），
  未确认按文体猜测（学术 4000 / 小说 3000 / 演讲稿 1200…），写作前必须对齐。
- 素材/论点门槛随篇幅动态放大：1000 字 → 素材 ≥3；3000 字 → 素材 ≥9、约 8 节。
- 大纲生成注入【篇幅预算】：按目标字数拆节，每节必须分配用户素材，缺素材的节要标"需补充"。
- 大纲评审新增硬检查：节数低于预算 → high；每节 >550 字 → mid；无素材的节 → high；素材闲置 → mid。
- 扩写纪律：优先写透本节分配素材，素材写尽仍不足 → 标注缺口让用户补，禁止空转套话凑字数。

### Added（结构性 AI 痕迹检测：从《差生》长稿审计提炼）

- `audit()` 新增 `structuralSignals`：章节开头单句定场（≥60% 章 ≤8 字开头）、章节结尾金句收束、
  三连排比（连续 6 句同句首 ≥3）、同语反复（"A是A，B是B" ≥3）、重复动作模板（"数…，数到…" ≥3）、
  内心话"不出口"收束（≥3）、单字句意象重复（"白的。"句尾 ≥3）、对话口头禅（同句 ≥5）。
  命中即进 suggestions 与 `--fix` 修订清单（结构性痕迹一起改，不只改单句）。
- 修复重复比喻漏检：喻体归一化剥离"在/着/正"等衬字——"像赶火车"与"像在赶火车"现在算同一喻体。
- 红队日志与 MCP 输出带 `structuralSignals` 计数。

## 0.16.0 (2026-08-07)

### Added（动态规划：文体驱动的澄清蓝图，不再一套 9 项框死所有写作）

- **genreBlueprint(name)**：每类文体定义自己的澄清维度——公文系问"事由/主送/依据/事项要点"
  （不问立意/论点/情感）；合同问"当事人/标的/条款要点"；小说（新增文体，含欧亨利式
  反转/伏笔骨架）问"情节架构/反转落点"；论文才要"支撑论点×N"；散文/演讲稿/记叙文
  默认蓝图去掉"论点"，保留主题/立场/读者/素材/立意/情感/结尾/风格底稿。
- **澄清状态机动态化**：`missingNeed` / 核心门槛 / 蓝图确认 / 访谈清单全部按当前文体蓝图
  计算；提问提示词注入【本蓝图字段】，LLM 只问蓝图里有的维度；
  `classifyAnswer`/`applyAnswer` 支持新字段（items/recipient/basis/plot）。
- **大纲门槛动态化**：gate() 按文体判断——散文不再强制"支撑论点×2"，
  议论文/学术/报告才要求；公文要求事项/依据。
- **访谈清单动态渲染**：`interview` 的确认清单随文体变化（通知 → 事项/主送，无论点）。
- 新增文体「小说」（欧亨利式：开场→冲突→伏笔→反转→余味）。

### Changed

- 版本 0.15.0 → 0.16.0（CLI HELP / MCP serverInfo / package.json / 插件清单同步）；
  e2e 202 项全绿。

## 0.15.0 (2026-08-07)

### Added

- **对话级双风格提炼**：澄清收尾自动把用户全部发言（素材/感受/修改意见）做 LLM 综合，
  提炼"人想写的（write）"与"人想听的（read）"双风格 + 联想库 + 惯用手法 +
  write-read 差异（如"想写克制低气压，读者需要最后一点亮的余味"），合并进档案并带
  "对话整体提炼"证据——**用户没贴旧稿也能在进入大纲前建立高层次风格档案**
  （《再见》测试暴露的根因：深层提取只对旧稿样本跑，对话里的物象/暗喻/收束偏好被浪费）。
- **深度审阅 Review（核心闭环）**：`stylotrace review [--fix]` 聚合红队审计 + 校对 +
  事实核查 + 原创性 + 风格保真 + 读者群像/交锋，输出 **P0 硬伤 / P1 建议 / P2 争议 /
  读者亮点**；`--fix` 自动修复 P0（红队修订 + 风格低分按建议重写）并复检。
  支持 `--file`（外部文稿，修复写回目标文件）与 `--quick`。MCP 工具 35 → 36（`review`）。

### Changed

- 版本 0.14.0 → 0.15.0（CLI HELP / MCP serverInfo / package.json / 插件清单同步）。

## 0.14.0 (2026-08-07)

### Added

- **凭据自动发现** `credentials.js`：未显式配置 `STYLOTRACE_LLM_API_KEY` 时，自动读取宿主
  已配置的可用 API——Codex `~/.codex/config.toml`（model_providers 的 `base_url` +
  `experimental_bearer_token`/`env_key`）、Claude Code settings.json（env 块）、
  OpenCode 配置、常见 `*_API_KEY` 环境变量。显式 `STYLOTRACE_LLM_*` 永远优先；
  只自动采用 OpenAI 兼容协议（Anthropic 仅检测提示）；密钥绝不打印（只显示来源与末 4 位）。
  `STYLOTRACE_CREDENTIALS=auto|ask|off` 控制模式（默认 auto）。
- **CLI `credentials`**：列出/采用候选（`--use N`）、交互选择或手动输入（`--ask`，
  保存到 `.stylotrace/credentials.json`，0600）、`--clear` 清除；`doctor` 显示凭据来源（脱敏）。

### Changed

- 版本 0.13.0 → 0.14.0（CLI HELP / MCP serverInfo / package.json / 插件清单同步）；
  e2e 全绿（含凭据脱敏/Codex 解析/显式优先/0600 存取断言）。
- **装完即用增强**：`agent` / `interview` / `clarify` 自动初始化工作区（无需先 `init`）；
  `install.sh` 安装完成后**自动注册本项目**（Codex 项目级 MCP + skill + 凭据，零手动、
  零全局副作用）；`--no-setup` 跳过、`--setup-all` 同时注册 Claude Code/OpenCode。
  新开对话直接说需求即启动。

## 0.13.0 (2026-08-07)

### Added

- **联网 RAG** `rag.js`：事实核查 verify 项自动生成检索查询；配置
  `STYLOTRACE_RAG_ENDPOINT/STYLOTRACE_RAG_API_KEY` 时直连 `POST /search {queries}` 并回灌，
  否则把 web-search 请求写入 `protocol/requests.jsonl`（宿主代检，`stylotrace rag ingest`
  或 MCP `rag_ingest` 回灌缓存与素材）。缓存 `vault/rag-cache.json`；
  CLI `rag status/search/ingest`，MCP `rag_search/rag_ingest`。
- **静默内部质量门（真实触发、不刷屏）**：导演交付前自动执行——风格保真评估（低分自动
  微调最多 2 轮）、原创性检查（文内重复句/与个人库自我复用/模板句）、校对扫描、事实核查 +
  RAG 检索请求；全部写入 `state.quality` 与 context 日志，交付消息不再罗列评估过程。
- **内置原创性检查** `originality.js`：`stylotrace originality [--file]` 手动查看；
  交付前静默自动执行，MCP `originality`。

### Changed

- 版本 0.12.0 → 0.13.0（CLI HELP / MCP serverInfo / package.json 同步）；
  MCP 工具 32 → 35；e2e 180 项全绿。

## 0.12.0 (2026-08-07)

### Added（P1 四项：改写矩阵 / 版本快照 / 全局档案 / 引文管理）

- **一键改写矩阵** `transform.js`：expand 扩写 / condense 缩写（`--target` 指定字数）/
  continue 续写 / polish 润色 / imitate 仿写 / tone:formal|casual|warm|authoritative
  改语气。复用 restyle 的分节改写 + 退让协议（独立实现，不耦合既有模块），改写前后自动快照。
- **版本快照 + 回滚** `history.js`：write / restyle / redteam --fix / transform 前自动
  快照到 `vault/history/`（内容相同跳过，最多 30 份）；`stylotrace history` / `rollback [N]`
  （回滚前先存当前版本）。修复 redteam-fix 与 rollback 后草稿哈希不同步、导致误判"外部修改"的问题。
- **全局风格档案** `profile.js`：`stylotrace profile export/import` —— 导出 write/read 档案、
  旧稿样本、修改记录、适配卡为 bundle；导入合并语义保守（本地高置信维度不覆盖、证据并集、
  样本/修改按内容去重、本地适配卡不覆盖）。
- **引文管理**：`stylotrace citations [--file]` 提取文中《引文》清单；
  `--append refs.json [--style gbt7714|apa]` 先快照再追加参考文献附录。
- MCP 工具 26 → 32（transform / history / rollback / profile_export / profile_import / citations）；
  e2e 170 项全绿。

### Changed

- 版本 0.11.0 → 0.12.0（CLI HELP / MCP serverInfo / package.json 同步）。

## 0.11.0 (2026-08-07)

### Added（补齐 P0：语音口述 / 格式与导出 / 校对纠错）

- **语音口述**：`stylotrace dictate <音频...> [--to-draft]` —— whisper/whisper.cpp 转录为素材
  （`STYLOTRACE_WHISPER_CMD` 可指定命令；默认自动检测；`--to-draft` 整理成口述草稿）；
  未配置转录器时明确降级提示。外部进程带超时，绝不阻塞主流程。
- **文体库 +4**：学术论文（摘要/关键词/引言/方法/结果/结论/参考文献）、新闻稿（5W1H 导语、
  倒金字塔）、邮件（主题/称呼/正文/落款）、视频脚本（【画面】【旁白】【字幕】分列）；
  个人写作库自动分类同步支持。
- **参考文献生成**：`stylotrace cite "<条目>" --style gbt7714|apa` —— 期刊/图书/网页/
  报纸/学位论文/报告，GB/T 7714-2015 与 APA 7 确定性格式化。
- **多格式导出**：`--html`（纯 Node 零依赖）、`--pdf`（reportlab 内置中文 CID 字体）、
  `--srt`（视频脚本台词转字幕，4 字/秒估算）、`--academic`（学术 docx：宋体小四/黑体标题/
  1.5 倍行距）。
- **校对纠错**：`stylotrace proofread [--file]` —— 错别字/易混词/叠字/标点/引号配对
  （确定性、毫秒级）+ 语病/搭配（LLM，apiKey 守卫）；`redteam --proofread` 同跑；
  导演交付时确定性校对并提示"N 处需核对"。
- **顺手修复**：`extractInput` 对图片/音频返回 Promise，ingest/clarify 之前未 await
  会静默失败——已改为异步并 await；`dictate` 不再把音频文件误当工作区。

### Changed

- 版本 0.10.0 → 0.11.0（CLI HELP / MCP serverInfo / package.json 同步）；
  MCP 工具 25 → 26；e2e 154 项全绿。

## 0.10.0 (2026-08-07)

### Changed（去臃肿：评估拆进每一轮，不做交付前大考）

- **新增风格脉搏（Style Pulse）**：`style-pulse.js` —— 澄清每轮、大纲生成后、每节写作后
  的轻量风格采集与即时反馈（确定性、零 LLM、几十毫秒）；每节写作的脉搏建议自动注入
  **下一节**提示词，问题不带进下一节。记录落 `vault/style-pulses.jsonl` + `state.stylePulses`；
  `stylotrace style --pulses` 可查看。
- **用户修改建议 = 评估反馈**：`applyCorrectionFeedback` —— "这句太文艺了/太啰嗦/更口语/
  结尾收一点"直接收紧/修正档案维度（含负置信微调）并记 correction 脉搏；导演收到这类建议后
  自动按它重写全文（不再只回"要改哪一处？"）。大纲修改意见同样吸收。
- **导演交付链去重**：移除交付前全稿 style-eval 自动环节（每节写作已即时评估）；交付消息
  不再罗列评估过程；风格适配卡改为"素材有变化才重蒸馏"；`STYLOTRACE_QUICK=1` 快速模式
  （读者 3 人、跳过交锋与适配卡重蒸馏）。
- 深度全稿评估保留为手动命令 `stylotrace style-eval`（不自动跑）。

### Changed

- 版本 0.9.0 → 0.10.0（CLI HELP / MCP serverInfo / package.json 同步）；e2e 141 项全绿。

## 0.9.0 (2026-08-07)

### Added

- **读者交锋辩论（MAJ-EVAL 式多智能体评审）**：8 位"第一读者"反应后，选分歧最大的
  3 位进入交锋——互看最尖锐意见 → 收敛出共识（直接改）/ 争议（作者拍板）/
  优先级（按对读者的伤害排序）；LLM 失败时确定性主题聚类兜底。
  CLI `stylotrace debate`，MCP 工具 `reader_debate`；导演交付链自动执行。
- **风格持续微调基建（Panza 式：<100 样本 + PeFT + RAG）**：
  `style-adapter.js` —— ① `--distill` 把旧稿/个人写作库/亲手修改对压缩成风格适配卡
  （写作/大纲/重写时最高优先级限量注入）；② `--dataset` 生成 Reverse Instructions 式
  偏好对 JSONL（point-edit 修改对即偏好对）；③ `--lora` 走 OpenAI 兼容微调接口
  （上传 /files → 创建 /fine_tuning/jobs），未配置端点时给出本地 LoRA 指引；
  新增 `scripts/finetune/style_lora.py`（torch+transformers+peft 本地训练）。
  CLI `stylotrace style-adapter`，MCP 工具 `style_adapter`；导演交付时自动蒸馏适配卡。
- **事实核查**：`fact-check.js` —— 确定性扫描数字/年代/引文/人名/机构（零 LLM），
  LLM 复核分级 material（来自素材）/ common（低风险）/ verify（交付前必须核对）；
  记录落 `vault/fact-check.jsonl`。CLI `stylotrace fact-check`，MCP 工具 `fact_check`；
  导演交付时确定性扫描并在交付消息里提示"N 处需核对"。
- **风格评估集成评分**：LLM 判断 + 确定性统计按 0.82/0.18 加权（参考 arxiv 2508.06374：
  集成指标优于单一指标）。

### Changed

- 版本 0.8.0 → 0.9.0（CLI HELP / MCP serverInfo / package.json 同步）；
  MCP 工具 22 → 25；导演交付链：… → 风格保真评估 → 读者群像 → **读者交锋** →
  交付（含事实核查提示 + 风格适配卡自动蒸馏）。

## 0.8.0 (2026-08-07)

### Added

- **风格保真评估闭环（Style Fidelity Eval）**：交付前自动对照作者本人的旧稿样本 +
  亲手修改对给草稿打"像不像你"的分数（参考 EMNLP 2025《Catch Me If You Can? Not Yet》
  与 WritingPreferenceBench 的偏好对比思路）。LLM 逐句对照打分（14 维），失败时确定性
  统计兜底（词汇重叠/句长分布/反 AI 黑名单），评估历史落 `vault/style-eval.jsonl`；
  低分时把漂移证据写回风格档案（只加 evidence、不覆盖维度值）。导演交付链自动执行，
  低分自动针对性重写一轮。CLI `stylotrace style-eval`，MCP 工具 `style_eval`。
- **大纲评审-修订回路（CogWriter / WriteHERE 式规划→评审→重规划）**：大纲生成后按
  立意贯穿/论点-功能匹配/逻辑递进/素材利用/篇幅分配/文体规范评审，低分且有 LLM 修订版
  时自动替换（用户仍需最终确认）；评审记录入 `state.outlineReviews`。
  CLI `stylotrace outline-review`，MCP 工具 `outline_review`。
- **公文国标升级**：文体库补全党政机关公文 15 文种（请示/批复/函/通报/公告/通告/意见/
  决定/决议/命令/公报/议案 + 原通知/纪要/报告/公文）；`stylotrace genre` 展示
  GB/T 9704-2012 排版规范；`stylotrace export --official [--redhead]` 按国标导出
  A4 公文 docx（37/35/28/26 页边距、2号小标宋标题、3号仿宋正文、黑体/楷体层级标题、
  右空四字落款、一字线页码、红头可选）。

### Changed

- 版本 0.7.0 → 0.8.0（CLI HELP / MCP serverInfo / package.json 同步）；
  MCP 工具 20 → 22；导演交付链：红队通过 → 风格保真评估 → 读者群像 → 交付。

## 0.7.0 (2026-08-07)

### Added

- **文体库（公式化内容）**：公文/合同/通知/会议纪要/报告/议论文/散文/演讲稿/记叙文——
  每种有结构骨架与行文规范（`stylotrace genre <名称>` 查看）；对话中识别文体
  （"写一份关于××的通知/合同"）并在大纲/写作/重写阶段按范式产出。
- **个人写作库**：作品自动分类归档 `vault/library/<类别>/`（议论文/散文/公文/合同…），
  蒸馏成"这类文体你个人的写法"（`vault/skills/personal/<类别>.md`），写作时限量注入
  （不污染上下文）；`stylotrace library / scan / view / add` 供查看与维护。
- **多模态输入输出**：对话中给文件路径自动提取（docx 用 python-docx、xlsx 用内置
  zipfile 解析、图片走视觉模型 STYLOTRACE_VISION_MODEL）；`stylotrace ingest` 提取素材、
  `stylotrace export` 导出 docx（导演交付自动导出 draft.docx）。
- 导演交付后自动：归档作品 → 蒸馏个人 skill → 导出 docx。

### Changed

- 版本 0.6.0 → 0.7.0（CLI HELP / MCP serverInfo / package.json 同步）。

## 0.6.0 (2026-08-06)

### Added

- **导演模式（Director，自主决策 · 主导对话）**：`stylotrace agent`（或 MCP `agent_step`）
  每次收到用户消息自动决定并执行下一步——澄清→大纲→逐节写作→反 AI 审计→读者群像→交付，
  用户不用催"继续"；只在真正的用户决策点停下（主题/立场/素材/立意/论点/大纲确认/风格方向）。
  交付后说风格方向 → 全文按新方向重写并再走一轮审计与群像。MCP 工具 19 → 20。
- **仓库纯净化**：移除旧控制台/Web/并行 TS MCP 残留（`integration/engine-mcp`、
  旧安装器 `scripts/install.sh`）；主仓库重组为 agent 单包（`packages/stylotrace` → 根）。
- **install.sh 定位修复**：按脚本自身位置判断本地仓库，任意目录调用都成立。

### Changed

- 版本 0.5.0 → 0.6.0（CLI HELP / MCP serverInfo / package.json 同步）。
- 根 package.json 增加 husky/lint-staged/commitlint，pre-commit = lint-staged + 全量 e2e。

## 0.5.0 (2026-08-06)

### Added

- **skill 内嵌完整引擎**：`skills/stylotrace/scripts/engine/` 是 agent 的完整快照
  （bin + src + templates + package.json），`scripts/stylotrace.mjs` 为启动器——
  装完 skill 即拥有全部工作流（interview/outline/write/redteam/audience/dissect/
  restyle/style/point-edit/mcp），**不再依赖外部安装的 stylotrace CLI**。
- **引擎同步脚本** `scripts/sync-skill-engine.sh`：agent/ 为单一事实源，
  `--check` 模式供 CI 校验漂移。
- **一键安装** `install.sh`：curl | bash 或 git clone；默认目录级安装
  （`<项目>/.codex/skills/stylotrace`），可 `--global` / `--cli` / `--mcp-codex`；
  已有安装自动备份（`.bak.<时间戳>`）可回滚；装完自动验证引擎可独立运行。
- **hook 命令**：宿主生命周期事件（session/user/assistant/compact/stop）→
  观察日志 + 压缩守卫（压缩前刷新风格指纹）。
- **checklist 命令**：渲染需求访谈确认清单（不消耗 LLM）。

### Changed

- 版本 0.4.0 → 0.5.0（MCP serverInfo、CLI HELP 同步）。
- `setup` 支持双布局（独立包 / skill 内嵌引擎），自动定位 skill 目录。

## 0.4.0 (2026-08-06)

### Added

- **需求访谈 `stylotrace interview`**：多轮一问 + 实时确认清单（✓/…、进度 x/9、剩余项），
  收尾打包"确认清单 + 风格档案进度 + 剩余步骤"；与 clarify 共享同一状态机。
- **读者群像 `stylotrace audience`**：8 个"第一读者"（老教师/挑剔编辑/中学生/挑剔评论家/
  焦虑家长/历史爱好者/随性读者/年轻作家）逐段记录第一次阅读的心理反应，交付前强制环节；
  LLM 不可用时确定性兜底，永不缺席。
- **定点引用 `stylotrace quote`**：一键生成可粘贴的「〔Stylotrace 引用〕《原句》/修改指令」块；
  `point-edit` 支持两行引用块单参数粘贴。
- **风格全程被动采集**：每句话/素材/修改理由即时写入 write/read 档案（带证据）；
  同文体旧稿（≥80 字）自动落盘并做 14 维风格提取（联想/技巧/注意力焦点）；
  `stylotrace style [--backfill|--extract]` 让"风格被读到了"全程可见。
- **风格记忆检索（RAG 增强注入）**：写作/大纲/扩写/红队修订前按
  "论题 + 文体 + 本节论点 + 高置信风格维度"检索作者旧稿片段与亲手修改对
  （原文→修改→意图），BM25 中文二元组打分，相关度/时间衰减/重要性加权排序，
  以少样本 + 联想库 + 反例块注入提示词；CLI 新增 `stylotrace style --memory <查询>` 预览，
  MCP 新增 `style_memory` 工具（17 → 18 个）。
- **整篇文章蓝图（grilling 式共同理解）**：澄清全程维护蓝图（主题/为什么写/核心张力/
  读者带走什么/结构顺序/论点/素材/情感/结尾），核心信息齐后回显整篇蓝图请用户确认，
  修正意见带进大纲生成；追问设计师被要求"每个问题都是蓝图的下一个拼图"。
- **风格方向与全文重写**：用户说"整篇更克制/更豪迈…"即时记录 `styleDirections` 并标记
  需要重写；CLI/MCP 新增 `restyle`（缺省读取最近方向，全文或指定节重写，保留结构与论点）；
  `stylotrace style --export` 导出人类可读档案 `vault/style-profile.md`。MCP 工具 18 → 19。
- MCP 新增 `interview_step / audience / quote / style_status`（13 → 17 个工具）。
- skill 独立形态新增 `checklist / quote` 子命令（零依赖）。

### Changed

- 澄清阶梯末尾补"风格底稿"一问（同文体旧稿，问一次即可，没有就放过）。
- `agent/package.json` 打包包含运行必需的 `templates/` 与 `README.md`。

## 0.3.0 (2026-08-06)

## 0.3.0 (2026-08-06)

### Added

- 完整 Agent CLI（零依赖）：澄清（立意/论点深挖、单问句强制）、带论点挂载的大纲、
  双风格写作（字数门槛/扩写）、确定性红队、感性解剖、定点修改（并发守卫）。
- 生态位探测器 `stylotrace probe`：主动触发判断，提议一次、被拒即退让。
- MCP stdio 服务器（13 个工具），供 Codex / Claude Code / OpenCode 调用。
- `stylotrace setup`：一键自动接入（检测宿主 → 原生注册 → 项目级 skill → 凭据复用 0600）。
- 引擎 MCP 接入包（integration/engine-mcp）：把原引擎全部深度暴露为 MCP。
- macOS 右键"在 Stylotrace 中修改"服务（extras/）。
- 一键安装：`curl -fsSL …/install.sh | bash`；项目级注册，绝不写全局。

### Changed

- 触发纪律升级为"主动感知生态位 + 退让底线"；未答问题一律不默认。
- 澄清硬门禁：主题 + 立场 + 素材≥2 + 核心立意 + 支撑论点≥2。

### Security

- `.env.local` 加入 .gitignore；凭据复用仅限本机已有配置，0600 写入。
