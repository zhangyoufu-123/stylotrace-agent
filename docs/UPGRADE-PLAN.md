# STYLOTRACE 全面升级方案（Phase 1 已完成 · 待推进 Phase 2/3）

> **状态更新（v0.68）**：Phase 1 已全部落地并合入（命名校准 A1、RSA 形式化 A2、学习曲线 A3、
> 论文重排 A5、个人回避库 B1、上下文窗口编辑对 B2、可解释层 B7、embedding 合并 C1、doctor C4、
> 盲评统计 D1）。核心命名已按用户要求定为 **改迹调制（revision-trace modulation）**，不再使用
> "ACCM" 等生硬缩写；调制器已到 **十二维**（含 avoidance 第 12 维）。以下清单中 A1 的旧命名与
> 旧维度计数为历史记录，实际以 CHANGELOG / HANDOVER-v0.68 为准。

> 原则：不追"解码器顶会"，把创新主线做实为
> **修订监督适应（Revision-Supervised Adaptation）+ 可解释调制 + 姿态层诊断**。
> 所有升级必须保持"23 套 agent 测试 + 11 套 web 测试全绿"的回归底线；
> 重写只发生在有明确理由、且行为契约不变的模块。

## 0. 现状基线（v0.67）

- 核心：四层风格签名 → 个人 n-gram → 外层调制器（十一维特征 + 偏好对权重学习 +
  增量在线更新 + embedding 原型 + L3 作者清单 + 姿态层 posture）；
- 写作：`write.js → decodeSection`（候选级生成 n 候选 → 评分 → 选优）；
- 交互：澄清协议 + 外溢优先 + 作者写作清单 + 修改监督（edits.jsonl）；
- 质量：姿态层六层细读（LLM）+ 反 AI 审计 + 学术规范审计 + 全流程互操作；
- 工程：CLI + MCP + Web（零依赖 Node + 原生前端）、23+11 套测试、技能三安装点；
- 已知问题（升级要解决的）：
  1. 论文"统一 Token 对比解码"名不副实（候选级，非逐 token）；
  2. 作者识别 81.7% < TF-IDF 基线 82.2%（不能当主成绩）；
  3. 无第三方盲评、无显著性检验；
  4. `style-vector.js` 与 `embedding.js` embedding 实现重复；
  5. `decodeSection` 评分与解释耦合，无法支撑可解释层与状态向量；
  6. 编辑对训练未用上下文窗口、未按意图分流——"修改即标注"尚未做到位；
  7. director.js 1200+ 行，决策不可追溯。

---

## 1. 升级项清单

### A. 理论 & 论文（对齐创新主线）

| 编号 | 升级项 | 现状 → 目标 | 重写/增量 | 代价 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| A1 | 命名校准 | "统一 Token 对比解码" → **改迹调制（revision-trace modulation）**，摘要/贡献/方法/未来工作全文一致 | 重写（文档） | 低 | P1 |
| A2 | 修订监督适应（RSA）形式化 | 把 edits.jsonl 的（原文→改后→意图）定义为"上下文局部偏好对"，给出损失、在线更新、样本复杂度命题，写入论文 3.x 与 MODULATOR.md | 增量 | 中 | P1 |
| A3 | 学习曲线实验 | n=5/10/20/40/80 编辑对 → 权重稳定性 + "改后>原文"排序正确率 + 拐点，输出曲线图与结论 | 新增脚本 | 中 | P1 |
| A4 | 姿态层诊断一致性实验 | 3 个独立 LLM 对同一文本六层细读的病灶一致性；确定性判据 vs LLM 判据的一致率 | 新增脚本 | 中（需 LLM 调用） | P3 |
| A5 | 论文结构重排 | 贡献一"修订即标注"、贡献二"可解释多信号调制"、贡献三"姿态层诊断学"；摘要第一句改为创作伙伴叙事 | 重写（文档） | 中 | P1 |

### B. 核心算法升级

| 编号 | 升级项 | 现状 → 目标 | 重写/增量 | 代价 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| B1 | 个人回避库（负空间第一阶段） | 聚合 edits.original 的"作者删掉什么"→ `vault/avoidance.json`（个人回避词/句式库）→ 调制器第 12 维 `avoidance` 特征 | 增量 | 低-中 | P1 |
| B2 | 上下文窗口化编辑对 | 训练偏好对时取原文前后各 80 字作上下文，缓解"短替换特征稀疏" | 增量（训练管线） | 中 | P1 |
| B3 | 意图分流训练 | 按 intent（风格/事实/格式/节奏）分组训练与加权；事实/格式修改不进风格统计 | 增量 | 中 | P2 |
| B4 | 状态向量化 | 调制器输入 t 从标量 → {进度, 情感浓度, 张力, 距结尾}，impedance 与特征族升级为状态依赖 | 增量 | 中-高 | P2 |
| B5 | 增量更新收敛控制 | 批量 vs 增量自动权衡（新对数量 vs 权重漂移），防单条错误修改带偏 | 增量 | 中 | P2 |
| B6 | V2 logprobs 探测 | 先写探测脚本验证 DeepSeek 等 API 是否返回 logprobs；支持才做词级重排，否则明确降级与文档化 | 新增脚本 → 决定 | 高（依赖外部 API） | P3 |
| B7 | 可解释层 | 得分分解 → 人话（候选级"为什么选它"：哪个信号主导、依据哪次修改），CLI + Web 面板 | 增量（依赖 C2） | 中 | P1 |

### C. 工程架构

| 编号 | 升级项 | 现状 → 目标 | 重写/增量 | 代价 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| C1 | embedding 统一服务 | 合并 `style-vector.js` 的 embedDense 与 `embedding.js`（双缓存/双实现 → 单实现单缓存），refreshStyleVector 引用统一服务 | 重写（小范围） | 低-中 | P1 |
| C2 | decodeSection 四段式重写 | 生成 → 特征 → 评分 → 解释 四段解耦，为 B7/B4 铺路；行为契约不变（返回值兼容） | 重写（核心） | 中-高 | P1/P2 边界 |
| C3 | 导演决策日志 | director 每次决策写 `director-log.jsonl`（决策类型/依据/轮次），可追溯可展示 | 增量 | 低 | P2 |
| C4 | 配置统一与 doctor 检查 | embedding/细读/搜索配置集中校验；`stylotrace doctor` 增加 embed/author-sheet 检查项 | 增量 | 低 | P1 |
| C5 | 技能同步自动化 | 固化 `scripts/sync-skill.sh`（替代手动 rsync）；CI 检查 skills 与 agent/src 一致 | 增量 | 低-中 | P2 |
| C6 | 测试体系扩展 | A2–A4/B1–B7 对应单测；e2e 拆"快速集 + 深度集"（当前全量约 3 分钟） | 增量 | 中 | 随各升级项 |
| C7 | 版本自动化 | version bump + CHANGELOG 模板脚本 | 增量 | 低 | P3 |

### D. 数据 & 评测

| 编号 | 升级项 | 现状 → 目标 | 重写/增量 | 代价 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| D1 | 盲评落地流程 | 已有问卷 + 二项检验 → 补一键收集（CSV 录入）+ 完整统计报告（精确二项 + 95%CI + 效应量），指导收集 10–20 人 | 增量 | 中 | P1 |
| D2 | 作者识别重定位 | 降为辅助证据；主实验改"续写选择 + 盲评"；可选跨文体语料 + 置换检验 | 增量（实验设计） | 中-高 | P2 |
| D3 | 多语言初测 | 英文风格特征最小支持（英文 n-gram/词表）+ 一篇英文章节测试 | 增量 | 中 | P3 |
| D4 | 真实语料采集流程 | 脱敏脚本（人名/地名）+ 同意模板 | 增量 | 低 | P2 |

### E. Web & 体验

| 编号 | 升级项 | 现状 → 目标 | 重写/增量 | 代价 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| E1 | 调制器可视化 | 权重表 + 得分分解面板（基于 B7 数据） | 增量 | 中 | P2 |
| E2 | 作者清单交互 | author-sheet 五问在 Web 展示/编辑/确认 | 增量 | 中 | P2 |
| E3 | 成长可视化 | "越写越懂你"：编辑对数、权重变化、学习曲线折线 | 增量 | 中-高 | P3 |
| E4 | 性能 | 候选数自适应（按语料/时间预算）、embedding 批处理 | 增量 | 中 | P3 |

---

## 2. 建议重写清单（明确列出）

| 模块 | 重写理由 | 代价 | 回退性 |
| --- | --- | --- | --- |
| `embedding.js` + `style-vector.js` embedding 部分（C1） | 双实现双缓存，行为漂移风险 | 低-中 | 高（单测 + 23 套回归） |
| `decodeSection` 四段式（C2） | 评分与解释耦合，无法支撑可解释/状态向量 | 中-高 | 中（保持返回值契约，e2e 强回归） |
| 论文命名与结构（A1/A5） | 名不副实是最大被攻击点 | 中 | 高（md 源 + docx 重渲染） |
| director.js | **不建议重写**——改加决策日志（C3），架构稳定性优先 | — | — |
| Web 前端 | **不建议换框架**——保持零依赖，增量加面板 | — | — |

## 3. 分阶段路线

### Phase 1（核心创新做实；预计 2–3 轮连续工作）
- A1 命名校准 + A2 RSA 形式化 + A5 论文结构重排；
- A3 学习曲线实验（出图 + 拐点结论）；
- B1 个人回避库 + B2 上下文窗口编辑对 + B7 可解释层（依赖 C2 先行或并行）；
- C1 embedding 合并 + C4 doctor 检查；
- D1 盲评流程（工具 + 报告模板，样本由你收集）。
- **交付**：论文 v0.68（新命名 + RSA 章节 + 学习曲线图）、盲评报告模板、十二维调制器（+avoidance 第 12 维）。

### Phase 2（状态与评测补强）
- B3 意图分流 + B4 状态向量 + B5 增量收敛；
- C2 decodeSection 四段式 + C3 导演日志；
- D2 作者识别重定位/置换检验；D4 语料脱敏；
- E1 调制器可视化 + E2 作者清单交互。

### Phase 3（边界扩展）
- B6 V2 探测（结果决定是否实现）+ A4 姿态层一致性实验 + D3 多语言 + E3/E4 + C7。

## 4. 风险与取舍

1. **范围失控**是最大风险：Phase 1 是承诺线，Phase 2/3 按需推进；
2. **C2 重写回归风险**：必须保持 23+11 套全绿，否则回退；
3. **B6 依赖外部 API**：先探测，不支持就文档化降级，不硬做；
4. **B4 数据依赖**：状态向量需要 outline/curve 结构稳定，先确认再动；
5. **D1 盲评需要真人**：工具与报告我们备好，样本只能你组织收集；
6. **诚实底线**：论文所有新意表述都保留"据我们所知"式的限定，不写"首创/空白"。

## 5. 需要你拍板的问题

1. Phase 1 范围（A1–A3/A5 + B1/B2/B7 + C1/C4 + D1）是否同意？
2. `decodeSection` 四段式重写是否授权（保持行为契约）？
3. B6（V2 logprobs）：先做探测脚本，结果决定做不做——可以吗？
4. Web 可视化（E1/E2）放 Phase 2 还是提前到 Phase 1？
