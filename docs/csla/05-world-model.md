# 世界/任务模型（05）— C4 修正

- MVP 用结构化/符号状态预测器（任务状态机上的转移 + 不确定性），latent 后置；
  latent 必须带 stop-grad/对比/重建防塌缩。
- 预测目标是"决策相关潜在状态"，不预测像素/全文。
- 误差双通道（C2）：δ^W（world）与 δ^R（reward）分别路由，禁止量纲混加。

实现状态：文档已定，代码未实现（暂无 world model 模块）。
